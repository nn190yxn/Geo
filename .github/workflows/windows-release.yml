name: Windows Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  package-windows:
    runs-on: windows-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.22.0
      - name: Prepare Node dependencies and builds
        shell: pwsh
        env:
          DATABASE_URL: postgresql://geo:geo@localhost:5432/geo_platform?schema=public
        run: |
          $ErrorActionPreference = 'Stop'
          New-Item -ItemType Directory -Path release -Force | Out-Null
          $logFile = Join-Path $env:GITHUB_WORKSPACE 'release\node-build.log'
          function Invoke-LoggedCommand {
            param(
              [string]$Label,
              [scriptblock]$Command
            )
            Write-Output "::group::$Label"
            & $Command *>&1 | Tee-Object -FilePath $logFile -Append
            $exitCode = $LASTEXITCODE
            Write-Output '::endgroup::'
            if ($exitCode -ne 0) { throw "$Label failed with exit code $exitCode." }
          }
           # The installer embeds Electron, which is a root devDependency.
           Invoke-LoggedCommand 'Install Node dependencies' { npm ci --include=dev }
          Invoke-LoggedCommand 'Download Electron runtime' { node ./node_modules/electron/install.js }
          if (-not (Test-Path 'node_modules\electron\dist\electron.exe')) {
            throw 'Electron runtime download did not produce electron.exe.'
          }
          Invoke-LoggedCommand 'Generate Prisma Client' { npm run prisma:generate --workspace @geo-platform/api }
          Invoke-LoggedCommand 'Build shared types' { npm run build --workspace @geo-platform/shared-types }
          Invoke-LoggedCommand 'Build API' { npm run build --workspace @geo-platform/api }
          Invoke-LoggedCommand 'Build Web' { npm run build --workspace @geo-platform/web }
          Invoke-LoggedCommand 'Run desktop tests' { npm run desktop:test }
          $requiredOutputs = @(
            'packages\shared-types\dist',
            'apps\api\dist',
            'apps\web\dist'
          )
          foreach ($output in $requiredOutputs) {
            if (-not (Test-Path $output)) { throw "Required build output is missing: $output" }
          }
      - name: Upload Node build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: windows-node-build-log
          path: release/node-build.log
          if-no-files-found: warn
      - name: Assemble desktop payload
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $logFile = Join-Path $env:GITHUB_WORKSPACE 'release\assembly.log'
          & pwsh -NoProfile -File ./deploy/windows/prepare-payload.ps1 -Workspace $env:GITHUB_WORKSPACE -RuntimeDirectory $env:RUNNER_TEMP *>&1 | Tee-Object -FilePath $logFile
          $assemblyExitCode = $LASTEXITCODE
          if ($assemblyExitCode -ne 0) {
            $details = (Get-Content $logFile -Tail 30) -join ' | '
            Write-Output "::error title=Desktop payload assembly failed::$details"
            exit $assemblyExitCode
          }
      - name: Upload assembly log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: windows-payload-assembly-log
          path: release/assembly.log
          if-no-files-found: warn
      - name: Validate Windows payload and scripts
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $scriptFiles = @(
            './deploy/windows/prepare-payload.ps1',
            './deploy/windows/validate-payload.ps1',
            './deploy/windows/Start-GEO.ps1',
            './deploy/windows/Stop-GEO.ps1'
          )
          foreach ($scriptFile in $scriptFiles) {
            $parseErrors = $null
            [System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path $scriptFile), [ref]$null, [ref]$parseErrors) | Out-Null
            if ($parseErrors.Count -gt 0) { throw "PowerShell syntax validation failed: $scriptFile" }
          }
          $validateLog = Join-Path $env:GITHUB_WORKSPACE 'release\validate.log'
          try {
            & pwsh -NoProfile -File ./deploy/windows/validate-payload.ps1 -PayloadRoot "$env:GITHUB_WORKSPACE\release\payload" 2>&1 | Tee-Object -FilePath $validateLog
            if ($LASTEXITCODE -ne 0) { throw "validate-payload.ps1 exited with code $LASTEXITCODE" }
          } catch {
            Write-Output "::error::Windows payload validation failed: $($_.Exception.Message)"
            if (Test-Path $validateLog) {
              Get-Content $validateLog -Tail 25 | ForEach-Object { Write-Output "::error::$_" }
            }
            exit 1
          }
      - name: Upload payload validation log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: windows-payload-validation-log
          path: release/validate.log
          if-no-files-found: warn
      - name: Smoke test Windows renderer payload
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $env:GEO_RENDER_SMOKE_ROOT = Join-Path $env:GITHUB_WORKSPACE 'release\payload\apps\web\dist'
          $electron = Join-Path $env:GITHUB_WORKSPACE 'node_modules\electron\dist\electron.exe'
          if (-not (Test-Path $electron)) { throw 'Electron executable was not found for renderer smoke test.' }
          $smokeLog = Join-Path $env:GITHUB_WORKSPACE 'release\renderer-smoke.log'
          $output = & $electron ./desktop/renderer-smoke.cjs 2>&1
          $smokeExitCode = $LASTEXITCODE
          $lines = @($output | ForEach-Object { "$_" })
          $lines | Tee-Object -FilePath $smokeLog
          if ($smokeExitCode -ne 0) {
            $details = (($lines | Select-Object -Last 30) -join ' | ').Replace('%', '%25').Replace("`r", '%0D').Replace("`n", '%0A')
            Write-Output "::error title=Windows renderer smoke test failed::$details"
            exit $smokeExitCode
          }
      - name: Upload renderer smoke log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: windows-renderer-smoke-log
          path: release/renderer-smoke.log
          if-no-files-found: warn
      - name: Install Windows packager
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $installLog = Join-Path $env:GITHUB_WORKSPACE 'release\inno-install.log'
          $existingIscc = Get-Command ISCC.exe -ErrorAction SilentlyContinue
          if (-not $existingIscc) {
            $knownPaths = @(
              (Join-Path ${env:ProgramFiles(x86)} 'Inno Setup 7\ISCC.exe'),
              (Join-Path ${env:ProgramFiles(x86)} 'Inno Setup 6\ISCC.exe'),
              (Join-Path ${env:ProgramFiles} 'Inno Setup 7\ISCC.exe'),
              (Join-Path ${env:ProgramFiles} 'Inno Setup 6\ISCC.exe')
            )
            $existingIscc = $knownPaths | Where-Object { Test-Path $_ } | Select-Object -First 1
          }
          if ($existingIscc) {
            Write-Output "Using preinstalled Inno Setup compiler: $($existingIscc.Source ?? $existingIscc)"
          } else {
            $installerUrl = 'https://github.com/jrsoftware/issrc/releases/download/is-7_1_0/innosetup-7.1.0-x64.exe'
            $expectedHash = '0362a383ed217d4c4239b5933866dd96d3eb2102737da92f80f6057a4b40df2f'
            $installer = Join-Path $env:TEMP 'innosetup-7.1.0-x64.exe'
            Invoke-WebRequest -UseBasicParsing $installerUrl -OutFile $installer
            $actualHash = (Get-FileHash $installer -Algorithm SHA256).Hash.ToLowerInvariant()
            if ($actualHash -ne $expectedHash) { throw "Inno Setup installer SHA256 mismatch: $actualHash" }
            $installProcess = Start-Process -FilePath $installer -ArgumentList '/VERYSILENT','/SUPPRESSMSGBOXES','/NORESTART','/SP-',"/LOG=$installLog" -Wait -PassThru
            if ($installProcess.ExitCode -ne 0) {
              if (Test-Path $installLog) { Get-Content $installLog -Tail 50 }
              throw "Inno Setup installation failed with exit code $($installProcess.ExitCode)"
            }
          }
      - name: Upload Inno Setup installation log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: windows-inno-install-log
          path: release/inno-install.log
          if-no-files-found: warn
      - name: Build EXE
        shell: pwsh
        env:
          GEO_RELEASE_VERSION: ${{ github.ref_name }}
          GEO_RELEASE_STAGE: ${{ github.workspace }}\release
        run: |
          $resolvedIscc = Get-Command ISCC.exe -ErrorAction SilentlyContinue
          $isccPath = $null
          if ($resolvedIscc) {
            $isccPath = $resolvedIscc.Source
          } else {
            $isccPath = @(
              (Join-Path ${env:ProgramFiles(x86)} 'Inno Setup 7\ISCC.exe'),
              (Join-Path ${env:ProgramFiles(x86)} 'Inno Setup 6\ISCC.exe'),
              (Join-Path ${env:ProgramFiles} 'Inno Setup 7\ISCC.exe'),
              (Join-Path ${env:ProgramFiles} 'Inno Setup 6\ISCC.exe')
            ) | Where-Object { Test-Path $_ } | Select-Object -First 1
          }
          if (-not $isccPath) { throw 'ISCC.exe was not found after Inno Setup installation.' }
          & $isccPath deploy/windows/GEO.iss
          if ($LASTEXITCODE -ne 0) { throw 'Inno Setup packaging failed.' }
      - name: Verify installer
        shell: pwsh
        run: |
          $installers = Get-ChildItem release/output/*.exe
          if ($installers.Count -ne 1) { throw 'Expected one EXE installer.' }
          if ($installers[0].Length -lt 1000000) { throw 'Installer is unexpectedly small.' }
      - name: Verify Windows install, startup, upgrade, and uninstall
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $installer = (Get-ChildItem release/output/*.exe).FullName
          $installRoot = Join-Path $env:ProgramFiles 'AI-Brand-Visibility-Assistant'
          $dataRoot = Join-Path $env:LOCALAPPDATA 'AI-Brand-Visibility-Assistant\data'
          $stateFile = Join-Path $dataRoot 'runtime-state.json'
          $sentinel = Join-Path $dataRoot 'release-e2e.txt'
          $installerLog = Join-Path $env:GITHUB_WORKSPACE 'release\installer.log'
          trap {
            $diagnostics = @("Desktop E2E failure: $($_.Exception.Message)")
            if (Test-Path $stateFile) {
              $diagnostics += '--- runtime-state.json ---'
              $diagnostics += (Get-Content $stateFile -Raw)
            }
            $desktopLog = Join-Path $dataRoot 'logs\desktop.log'
            if (Test-Path $desktopLog) {
              $desktopLines = Get-Content $desktopLog
              $truncated = $desktopLines | ForEach-Object { if ($_.Length -gt 400) { $_.Substring(0, 400) } else { $_ } }
              $diagnostics += '--- desktop.log (head 15) ---'
              $diagnostics += ($truncated | Select-Object -First 15)
              $diagnostics += '--- desktop.log (key lines) ---'
              $diagnostics += ($truncated | Where-Object { $_ -match 'Starting local services|Stopping local services|Local services failed|psql|inspection|createdb|Prisma|Nest|FATAL|ERROR|error|Error|failed|Failed|shutdown|connection|refused|EADDR|ECONN|ready|exit|WARNING|warning' -and $_ -notmatch 'Applying migration|RouterExplorer|RoutesResolver|InstanceLoader' } | Select-Object -First 80)
              $diagnostics += '--- desktop.log (tail 80) ---'
              $diagnostics += ($truncated | Select-Object -Last 80)
            }
            if (Test-Path $installerLog) {
              $diagnostics += '--- installer.log (tail 10) ---'
              $diagnostics += (Get-Content $installerLog -Tail 10)
            }
            if (Test-Path $installRoot) {
              $diagnostics += '--- leftover installation directory ---'
              $diagnostics += (Get-ChildItem $installRoot -Recurse -Force -ErrorAction SilentlyContinue | ForEach-Object { if ($_.PSIsContainer) { $_.FullName + '\' } else { $_.FullName + ' [' + $_.Length + ']' } })
            }
            Write-Output ('::error::' + (($diagnostics -join "`n") -replace "`n", '%0A'))
            exit 1
          }

          function Wait-ForReady {
            $deadline = (Get-Date).AddMinutes(2)
            $iteration = 0
            $lastStatus = $null
            while ((Get-Date) -lt $deadline) {
              $iteration++
              if (Test-Path $stateFile) {
                $state = Get-Content $stateFile -Raw | ConvertFrom-Json
                if ($state.status -ne $lastStatus) {
                  Write-Output "::warning::Wait-ForReady status=$($state.status) apiProcessId=$($state.apiProcessId) apiPort=$($state.apiPort) error=$($state.error)"
                  $lastStatus = $state.status
                }
                if ($state.status -eq 'failed') {
                  throw "Desktop application startup failed: $($state.error)"
                }
                if ($state.status -eq 'running' -and $state.uiReady -eq $true -and $state.apiProcessId -and $state.databasePort) {
                  try {
                    $response = Invoke-WebRequest -UseBasicParsing "http://127.0.0.1:$($state.apiPort)/api/v1/health/ready" -TimeoutSec 5
                    if ($response.StatusCode -eq 200) { return }
                  } catch {}
                }
              } elseif ($iteration % 20 -eq 0) {
                Write-Output "::warning::Wait-ForReady $($iteration)s: no state file yet"
              }
              Start-Sleep -Seconds 1
            }
            throw 'Desktop application did not render its UI and reach the readiness endpoint within two minutes.'
          }

          $installerArguments = @(
            "/DIR=`"$installRoot`"",
            "/LOG=$installerLog",
            '/VERYSILENT',
            '/SUPPRESSMSGBOXES',
            '/NORESTART',
            '/SP-',
            '/NOCANCEL',
            '/CLOSEAPPLICATIONS'
          )
          $installProcess = Start-Process -FilePath $installer -ArgumentList $installerArguments -Wait -PassThru
          if ($installProcess.ExitCode -ne 0) { throw "Initial installer failed with exit code $($installProcess.ExitCode)" }
          & (Join-Path $installRoot 'windows\Start-GEO.ps1')
          Wait-ForReady
          $state = Get-Content $stateFile -Raw | ConvertFrom-Json
          $apiHeaders = @{ 'x-user-id' = 'user_demo'; 'x-brand-id' = 'brand_demo' }
          $brandsResponse = Invoke-RestMethod -UseBasicParsing "http://127.0.0.1:$($state.apiPort)/api/v1/brands" -Headers $apiHeaders -TimeoutSec 10
          if (-not $brandsResponse.success -or -not ($brandsResponse.data | Where-Object { $_.brandId -eq 'brand_demo' })) {
            throw 'Seeded demo brand is unavailable after desktop startup.'
          }
          $dashboardResponse = Invoke-RestMethod -UseBasicParsing "http://127.0.0.1:$($state.apiPort)/api/v1/brands/brand_demo/dashboards/beginner-home" -Headers $apiHeaders -TimeoutSec 10
          if (-not $dashboardResponse.success) {
            throw 'Seeded demo dashboard request failed after desktop startup.'
          }
          New-Item -ItemType File -Path $sentinel -Force | Out-Null
          & (Join-Path $installRoot 'windows\Stop-GEO.ps1')

          $upgradeProcess = Start-Process -FilePath $installer -ArgumentList $installerArguments -Wait -PassThru
          if ($upgradeProcess.ExitCode -ne 0) { throw "Upgrade installer failed with exit code $($upgradeProcess.ExitCode)" }
          if (-not (Test-Path $sentinel)) { throw 'Upgrade removed persisted user data.' }
          & (Join-Path $installRoot 'windows\Start-GEO.ps1')
          Wait-ForReady
          & (Join-Path $installRoot 'windows\Stop-GEO.ps1')

          $uninstaller = Join-Path $installRoot 'unins000.exe'
          $uninstallProcess = Start-Process -FilePath $uninstaller -ArgumentList @('/VERYSILENT', '/SUPPRESSMSGBOXES', '/NORESTART') -Wait -PassThru
          if ($uninstallProcess.ExitCode -ne 0) { throw "Uninstaller failed with exit code $($uninstallProcess.ExitCode)" }
          $uninstallDeadline = (Get-Date).AddMinutes(2)
          while ((Get-Date) -lt $uninstallDeadline -and (Test-Path $installRoot)) {
            Start-Sleep -Seconds 1
          }
          if (-not (Test-Path $sentinel)) { throw 'Uninstall removed persisted user data.' }
          if (Test-Path $installRoot) { throw 'Uninstall left the application installation directory behind.' }
      - name: Collect Windows E2E diagnostics
        if: always()
        shell: pwsh
        run: |
          $source = Join-Path $env:LOCALAPPDATA 'AI-Brand-Visibility-Assistant\data'
          $destination = Join-Path $env:GITHUB_WORKSPACE 'release\e2e-diagnostics'
          New-Item -ItemType Directory -Path $destination -Force | Out-Null
          if (Test-Path $source) {
            Copy-Item $source $destination -Recurse -Force
          }
          $installerLog = Join-Path $env:GITHUB_WORKSPACE 'release\installer.log'
          if (Test-Path $installerLog) {
            Copy-Item $installerLog (Join-Path $destination 'installer.log') -Force
          }
          $installRoot = Join-Path $env:ProgramFiles 'AI-Brand-Visibility-Assistant'
          if (Test-Path $installRoot) {
            Get-ChildItem $installRoot -Recurse | Select-Object FullName, Length, LastWriteTime |
              ConvertTo-Json -Depth 2 |
              Set-Content (Join-Path $destination 'installed-files.json') -Encoding utf8
          }
      - name: Upload Windows E2E diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: windows-e2e-diagnostics
          path: release/e2e-diagnostics
          if-no-files-found: warn
      - uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: AI品牌曝光助手 ${{ github.ref_name }}
          generate_release_notes: true
          body: |
            AI品牌曝光助手 Windows 独立桌面应用包含 API、Web、Electron 和 PostgreSQL 本地运行时。
            安装后从开始菜单或桌面快捷方式打开应用，无需 Docker Desktop。
            用户数据和日志位于 `%LOCALAPPDATA%\AI-Brand-Visibility-Assistant\data`。
            当前安装包未进行 Windows 代码签名。
          files: release/output/*.exe
