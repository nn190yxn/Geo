# 需求实施计划

- [x] 1. 重构全局信息架构和新手入口
  - [x] 1.1 更新导航分组、路由命名和模块描述
    - 修改 `apps/web/src/layouts/navigation.ts`，将导航重组为“开始使用、监测对象、内容运营、发布运营、分析诊断”或等价的新手业务流程结构
    - 将 `品牌工作台` 调整为 `新手首页` 入口，将 `模型设置` 业务化为 `AI 平台`，将画布作为关系视图或高级视图入口
    - 保留现有路由能力和品牌上下文注入，避免破坏 `/brands/:brandId/*` 路由别名
    - 覆盖需求 1.1、1.2、12.1、12.4、12.5；对应设计 `Navigation Design` 和 `Page-Level Design/品牌工作台`

  - [x] 1.2 实现新手首页首屏任务工作台
    - 修改 `BrandWorkspacePage.tsx`，新增“从这里开始”区域，展示补品牌资料、创建监测对象、获取真实 AI 回复三步任务
    - 增加自然语言任务入口或问题式引导卡片，支持跳转到品牌资料、优化单元、用户意图、AI 回复监测和内容生成
    - 用当前 `BrandWorkspaceSnapshot`、`VisibilitySprint` 和现有计数生成待办状态、阶段进度和下一步动作
    - 覆盖需求 1.1、1.3、1.4、1.5；对应设计 `Page-Level Design/品牌工作台`

  - [x] 1.3 建立业务术语解释和显示标签封装
    - 新增或扩展 `apps/web/src/utils/displayLabels.ts`，集中维护优化单元、用户意图、信源分析、事实分析、内容引用、真实 AI 回复等解释文案
    - 在工作台、优化单元、用户意图、监测、内容和分析页面标题、副标题、空态和 tooltip 中复用业务解释
    - 确保 UI 展示豆包、Kimi、DeepSeek、通义千问、阶跃星辰等业务平台名
    - 覆盖需求 12.1、12.2、12.4、12.5；对应正确性属性 5、6

  - [x] 1.4 建立页面级 UI 设计骨架和组件清单
    - 在 `apps/web/src/styles/global.css` 中补齐新手首页、资产中心、对象列表、创作台、发布统计、分析诊断的页面级布局样式
    - 明确可复用 UI 组件形态：任务入口卡、下一步动作条、列表工具栏、筛选区、状态标签、诊断卡、平台统计卡、右侧详情面板、发布检查项
    - 为关键页面定义响应式约束：桌面端保持列表和详情并排，移动端按“标题说明 -> 主操作 -> 列表 -> 详情”顺序堆叠
    - 视觉上采用工作台型界面，优先使用紧凑列表、分组表单、工具栏和数据卡，控制装饰性卡片数量
    - 覆盖需求 1.1、1.2、5.1、6.3、7.1、12.4；对应用户补充“整体设计板块清晰、方便新手和企业工作人员使用”

  - [x]* 1.5 编写导航和术语显示测试
    - 更新 `navigation.test.ts`，覆盖新导航分组、关键路由、品牌化别名和工作流顺序
    - 为术语解释 helper 增加单元测试，覆盖平台名称、专业词解释和未知状态 fallback
    - 覆盖需求 1.2、12.1、12.2；对应正确性属性 5、6

- [x] 2. 建设品牌资产中心和资料完整度体验
  - [x] 2.1 扩展品牌资料库页面结构
    - 修改 `BrandKnowledgeCard.tsx` 和 `brandProfileLibrary.ts`，把品牌资料拆为基础信息、产品服务、目标用户、品牌知识、媒体资产、自有媒体账号、竞品信息七类
    - 每个分组展示完整度、缺失项、最近更新时间、可用于标准答案状态和补充入口
    - 对资料不足、上传失败、待确认字段使用可执行空态，引导用户编辑、上传或确认资料
    - 覆盖需求 2.1、2.5、2.6、12.3；对应设计 `Page-Level Design/品牌资料库`

  - [x] 2.2 设计品牌资产中心的分区交互和字段密度
    - 为基础信息区使用紧凑表单和右侧完整度摘要，字段覆盖品牌名称、官网、行业、地区、业务介绍、服务范围、联系方式和禁用表达
    - 为产品服务区使用可重复条目列表，字段覆盖产品名称、适用人群、使用场景、价格口径、优势、限制和 FAQ
    - 为目标用户区使用画像卡片列表，字段覆盖用户类型、决策阶段、关注问题、反对理由、常见搜索表达和高价值意图
    - 为知识库、图片素材、自有媒体和竞品信息使用列表页样式，提供搜索、类型筛选、状态筛选和主操作按钮
    - 覆盖需求 2.1-2.6、12.3；对应用户补充“板块清晰、企业人员能正常使用”

  - [x] 2.3 补齐品牌资料库前后端聚合接口
    - 在 shared-types 中定义 `BrandProfileLibrary`、`BrandProfileLibraryInput`、`BrandProfileLibrarySection` 和完整度摘要类型
    - 在 API 品牌模块增加 `GET /api/v1/brands/:brandId/profile-library` 与 `PATCH /api/v1/brands/:brandId/profile-library` 聚合接口，底层复用现有 `BrandProfile`、`KnowledgeSource`、`Competitor`、`PublishingAccount` 数据
    - 在 memory 和 Prisma repository 中实现聚合读取与增量保存，不改变真实密钥、cookies、storage state 的公开边界
    - 覆盖需求 2.1-2.6、11.3；对应设计 `Backend Interfaces` 和 `Data Models/BrandProfileLibrary`

  - [x] 2.4 增加图片素材和媒体资产最小模型
    - 在 shared-types 中定义媒体资产类型，字段包含素材类型、适用平台、适用内容、来源、审核状态和关联内容任务
    - 在前端品牌资料库中加入图片素材列表、上传占位入口和素材适用场景展示
    - 首版可复用 `KnowledgeSource` 或 `ContentAsset` 承接素材，不引入自动发布能力
    - 覆盖需求 2.1、2.5、5.2、6.4；对应设计 `Data Models/BrandProfileLibrary`

  - [x]* 2.5 编写品牌资料库单元和 API 测试
    - 为资料完整度、缺失项、分组进度和资料来源状态编写 Web helper 测试
    - 为 profile-library 聚合接口编写 API 测试，覆盖读取、更新、缺失字段和品牌隔离
    - 覆盖需求 2.1-2.6；对应测试策略 `品牌资料库读取和更新`

  - [x]* 2.6 编写资料完整度属性测试
    - Property P1：任意资料分组的完整度必须在 0 到 100 之间，覆盖需求 2.6 和设计 `Test Strategy/资料完整度计算`
    - Property P2：任意缺失字段必须能映射到补充入口或影响说明，覆盖需求 2.6 和错误处理 `资料缺失`

- [x] 3. 重构优化单元、用户意图和监测对象列表
  - [x] 3.1 改造优化单元列表为新手可执行对象页
    - 修改 `OptimizationUnitsCard.tsx`，增加页面说明：“优化单元是希望 AI 推荐的产品、服务或业务主题”
    - 增加搜索、类型筛选、优先级筛选、空态示例和新增按钮固定入口
    - 每行展示关联用户意图数、监测问题数、推荐度、平均排名、内容资产数和状态
    - 每行增加创建用户意图、开始监测、生成内容、查看分析、编辑等动作入口
    - 覆盖需求 3.1、3.2、4.1、5.1、12.3；对应设计 `GEO 画布` 和用户补充竞品逻辑

  - [x] 3.2 改造用户意图列表为真实问题管理页
    - 修改 `UserIntentPromptCard.tsx`，增加页面说明：“用户意图是客户可能向 AI 提出的真实问题”
    - 列表展示用户问题、关联优化单元、决策阶段、推荐度、平均排名、内容引用率、负面评价数、事实异常数、最近监测时间
    - 每行增加手动检测、自动监测、生成内容、查看检测记录、查看引用来源和编辑入口
    - 监测问题展开行增加无问题时的生成问题按钮和解释文案
    - 覆盖需求 3.3、4.1、4.6、5.1、9.2、10.2；对应设计 `MonitoringPlanWizard` 和 `GeoCanvasNodeDetails`

  - [x] 3.3 将关系视图作为对象列表后的高级视图
    - 修改 `GeoCanvasPage.tsx`，保留左侧优化单元列表、中间关系图、右侧详情面板
    - 页面标题和空态强调这是优化单元、用户意图、监测问题、平台表现、内容任务和复测结果的关系视图
    - 节点详情继续提供查看真实回复、生成内容任务、补充标准答案和再次监测入口
    - 覆盖需求 3.1-3.5；对应设计 `Page-Level Design/GEO 画布`

  - [x]* 3.4 编写监测对象 Web 测试
    - 覆盖优化单元空态、行操作、筛选项和新手解释展示
    - 覆盖用户意图行操作、监测问题展开空态和平台表现业务标签
    - 覆盖需求 3.1-3.5、4.1、12.3

- [x] 4. 重构 AI 回复监测创建、待补充队列和真实回复边界
  - [x] 4.1 实现监测计划创建向导 UI
    - 修改 `MonitoringPage.tsx`，新增“创建 AI 回复监测计划”向导，按优化单元、用户意图、监测问题、AI 平台、监测方式和频率组织
    - 监测方式显示自动监测、浏览器辅助监测、手动录入三类业务入口
    - 平台未配置时显示配置入口、手动录入入口和影响范围
    - 覆盖需求 4.1、4.2、4.3、11.5；对应设计 `Page-Level Design/AI 回复监测`

  - [x] 4.2 实现待补充真实回复队列
    - 在 `MonitoringPage.tsx` 中聚合缺少真实原始回复的监测项，展示问题、平台、缺少内容、推荐操作和处理入口
    - 保持 `mock_ai`、人工样例和标准答案不进入真实 AI 回复指标计算
    - 缺少真实回复时阻止该项进入指标计算，并引导浏览器辅助监测或手动录入
    - 覆盖需求 4.3、4.4、4.5、4.6；对应正确性属性 1、2

  - [x] 4.3 补齐监测运行公开状态和用户解释
    - 修改 `MonitoringRunsCard.tsx`、`ManualTestEntryCard.tsx`、`PlatformConfigCard.tsx` 和相关 helper
    - 将 API、浏览器、手动、失败、待确认、待补充状态转换成用户可理解的监测状态和下一步动作
    - 公开响应和前端展示只显示平台脱敏状态，不显示密钥、cookies、storage state 或 profile 路径
    - 覆盖需求 4.2-4.6、11.2-11.5、12.4；对应正确性属性 1、4、6

  - [x]* 4.4 编写真实回复边界单元和属性测试
    - Property P3：任意指标聚合只允许使用真实 API 回复、浏览器辅助结果或用户手动录入真实回复，覆盖需求 4.5 和正确性属性 1
    - Property P4：任意标准答案、内容草稿、mock 平台样例不得计入真实 AI 回复指标，覆盖需求 4.5 和正确性属性 2
    - 为待补充队列 helper 编写测试，覆盖自动监测、浏览器辅助和手动录入路径

- [x] 5. 建设内容生成、内容优化和内容资产运营链路
  - [x] 5.1 新增内容生成列表页结构
    - 修改或扩展 `ContentGenerationPage.tsx`，在创作台前增加内容任务列表视图或顶部列表区
    - 列表展示内容标题、内容模板、关联优化单元、关联用户意图、适用平台、状态、创建时间、发布统计和操作
    - 操作包含创建内容、继续编辑、发布准备、再次监测和查看统计
    - 覆盖需求 5.1、5.3、5.5、6.4；对应设计 `ContentStudio` 和用户补充三张媒体发文统计图

  - [x] 5.2 完善左配置右结果创作台 UI
    - 左侧配置包含内容目标、优化单元、用户意图、内容模板、发布平台、语气风格、引用资料、图片素材和合规要求
    - 右侧结果包含生成前空态、内容草稿、FAQ、引用依据、风险提醒、渠道发布检查和保存入口
    - 内容优化模式支持粘贴已有内容并输出结构建议、事实补强、FAQ 补充、引用补强和渠道适配建议
    - 覆盖需求 5.1-5.6、12.3；对应设计 `Page-Level Design/内容生成与内容优化`

  - [x] 5.3 细化内容创作 UI 状态和交互反馈
    - 创作台左侧配置按“目标与对象、模板与渠道、素材与依据、生成配置、发布检查”分组
    - 右侧结果区支持未生成、生成中、生成失败、待审核、已保存、已进入发布准备六类状态
    - 生成中状态展示涉及平台、当前步骤、预计下一步和保留任务后继续查看的提示
    - 发布检查区展示平台字数、封面图、素材、事实依据、引用依据、禁用表达和复测计划状态
    - 覆盖需求 1.3、5.1-5.6、6.4、12.4；对应用户补充三张内容创作和媒体发文图

  - [x] 5.4 建立内容模板和素材规则配置层
    - 在前端新增内容模板配置 helper，覆盖品牌介绍、问答科普、选购指南、竞品对比、案例故事、榜单推荐、本地攻略、FAQ 汇总、媒体稿、小红书种草、知乎长文和公众号文章
    - 为每类模板配置适用平台、推荐字数、素材要求、引用要求和复测建议
    - 模板选择结果写入现有内容生成任务输入或前端配置状态，不破坏后端内容生成契约
    - 覆盖需求 5.1、5.2、5.3、6.3；对应用户补充三张内容创作与媒体发布截图

  - [x] 5.5 补齐内容资产后端聚合接口
    - 在 shared-types 中扩展 `ContentAsset` 页面模型，包含优化单元、用户意图、sourceReferences、reviewStatus、publishStatus、retestPlanId 和发布统计摘要
    - 在内容模块或 BFF 聚合层提供 `GET /api/v1/brands/:brandId/content-assets` 与 `POST /api/v1/brands/:brandId/content-assets` 页面契约，复用现有内容资产、内容版本、发布记录和复测任务
    - 确保内容资产必须关联来源资料、优化单元或用户意图中的至少一类上下文
    - 覆盖需求 5.2、5.5、5.6；对应正确性属性 3

  - [x]* 5.6 编写内容链路测试和属性测试
    - 覆盖内容模板选择、素材规则、左右分栏空态、保存草稿和发布准备入口
    - Property P5：任意保存的内容资产必须至少关联来源资料、优化单元或用户意图中的一类上下文，覆盖需求 5.5 和正确性属性 3
    - 覆盖内容优化模式的事实补强、FAQ 补充和渠道适配提示

- [x] 6. 建设自有媒体、媒体平台和发文统计能力
  - [x] 6.1 重构发布中心为自有媒体和媒体平台双入口
    - 修改 `PublishingCenterPage.tsx`，按路由或页签区分自有媒体账号管理和媒体平台规则管理
    - 自有媒体展示官网、博客、公众号、知乎、小红书、B 站、视频号和其他账号配置状态
    - 媒体平台展示内容格式要求、适合用户意图、推荐发布频率、封面图比例和发布注意事项
    - 覆盖需求 6.1、6.2、6.3、12.4；对应设计 `Page-Level Design/自有媒体与媒体平台`

  - [x] 6.2 新增媒体账号和平台规则后端契约
    - 在 shared-types 中定义或扩展 `OwnedMediaAccount`、`MediaPlatformRule`、`PublishingChannelStats` 类型
    - 在发布模块增加 `GET /api/v1/brands/:brandId/owned-media`、`POST /api/v1/brands/:brandId/owned-media` 和平台规则聚合读取接口
    - memory 和 Prisma repository 复用 `PublishingAccount` 与 `PublishingRecord` 计算账号状态和发文统计
    - 覆盖需求 6.1-6.5；对应设计 `Backend Interfaces` 和 `Data Models/OwnedMediaAccount`

  - [x] 6.3 实现发文统计和渠道发布检查 UI
    - 在发布中心或内容生成列表中增加发文统计卡片，按公众号、小红书、知乎、B 站、官网、博客等平台展示发文数量、待发布数、已发布数、AI 引用次数、关联用户意图数和再次监测状态
    - 在内容发布准备中展示账号缺失、素材缺失、格式不匹配和待复测提醒
    - 覆盖需求 6.3、6.4、6.5、9.3；对应用户补充三张媒体发文统计图

  - [x] 6.4 设计媒体发布统计看板和平台明细页
    - 新增媒体发布统计看板，按平台展示发文总数、近 7 天新增、待发布、发布失败、已被 AI 引用、关联用户意图和待再次监测数量
    - 平台明细页展示该平台的内容列表、账号列表、素材规则、发文频率、最近发布时间和下一步建议
    - 内容列表支持按发布状态、内容类型、优化单元、用户意图、是否被引用和是否已复测筛选
    - 每条内容提供查看草稿、进入发布准备、记录发布结果、创建再次监测、查看引用表现入口
    - 覆盖需求 6.1-6.5、9.3、9.4；对应用户补充“不同媒体发文章的一些统计”

  - [x] 6.5 实现发布记录到 AI 引用和再次监测的聚合关系
    - 后端聚合 `PublishingRecord`、`ContentAsset`、`CitationSource`、`OptimizationTask` 和 `RetestRecord`，生成每个平台和每条内容的发布后表现摘要
    - 发布统计只展示可追溯的发布记录和引用记录，无法识别来源时使用“未识别来源”状态并提供人工补充入口
    - 再次监测状态按未安排、待执行、已执行、已改善、未改善展示，并能跳转到任务复测页面
    - 覆盖需求 6.4、9.3、9.5、10.3；对应设计 `SprintRetestService` 和 `PublishingCenterPage`

  - [x]* 6.6 编写发布运营测试
    - 覆盖自有媒体和媒体平台双模式切换、平台规则展示、发文统计计算和账号缺失提示
    - 覆盖发布准备阶段字段缺失提示和再次监测入口
    - 覆盖需求 6.1-6.5

- [x] 7. 统一分析诊断页面和可执行建议
  - [x] 7.1 建设分析诊断通用容器
    - 新增或扩展 `AnalysisWorkbench`，统一顶部诊断卡、趋势区、平台筛选、明细区和任务区
    - 诊断卡展示本期关键发现、影响范围、建议动作、风险等级和下一步按钮
    - 竞品、评价、信源、事实分析均复用该容器
    - 覆盖需求 7.1、8.1、9.1、10.1、12.4；对应设计 `Page-Level Design/分析诊断页面`

  - [x] 7.2 补齐四类分析聚合接口
    - 在 shared-types 中定义 `AnalysisFinding`、`AnalysisWorkbenchDashboard`、`AnalysisRecommendedAction` 类型
    - 在后端增加或扩展四类分析聚合接口：竞品、评价、信源、事实，底层复用 `AnalysisResult`、`Competitor`、`CitationSource`、`EvaluationIssue`、`BrandProfile` 和内容任务数据
    - 聚合结果必须包含优化单元、用户意图、平台、证据、严重程度、建议动作和关联任务 ID
    - 覆盖需求 7.1-7.4、8.1-8.4、9.1-9.5、10.1-10.4；对应设计 `Backend Interfaces` 和 `Data Models/AnalysisFinding`

  - [x] 7.3 实现分析明细到任务的动作入口
    - 竞品差距支持创建竞品改进任务并关联优化单元、竞品、用户意图和内容资产类型
    - 负向评价支持事实澄清、内容补强、FAQ 补充和复测任务入口
    - 高频信源支持创建官网页、FAQ、评测页或问答内容建议
    - 事实偏差支持更新标准答案、创建内容任务和安排再次监测
    - 覆盖需求 7.4、8.3、9.4、10.3、10.4

  - [x]* 7.4 编写分析聚合和 UI 测试
    - 覆盖四类分析页顶部诊断卡、平台筛选、明细展示和建议动作
    - 覆盖未识别来源、事实依据缺失、负向评价和竞品压制四类边界状态
    - 覆盖需求 7.1-10.4 和错误处理 `引用缺失`、`事实冲突`

- [x] 8. 完成全局空态、状态和视觉系统重构
  - [x] 8.1 统一空态和下一步动作组件
    - 扩展 `PageState.tsx` 或新增业务空态组件，统一展示当前缺少的数据、为什么需要它、建议下一步和主按钮
    - 替换品牌资料、优化单元、用户意图、AI 回复监测、内容、发布、分析页面中的泛化空态
    - 覆盖需求 1.4、2.6、4.5、6.5、9.5、10.4、12.3；对应错误处理 `资料缺失`、`平台未配置`、`引用缺失`

  - [x] 8.2 建立新手友好 UI 设计规范落地层
    - 更新 `global.css` 和关键页面布局，统一任务卡、诊断卡、列表工具栏、状态标签、侧栏配置、右侧结果面板和移动端响应式约束
    - 控制卡片层级，避免页面区块过度卡片化，列表页使用工具栏、筛选、表格和行操作承接主要工作
    - 内容创作页保持左配置右结果，分析页保持顶部诊断卡、中部趋势、底部明细
    - 覆盖需求 1.1、5.1、5.3、7.1、8.1、9.1、10.1、12.4；对应用户对竞品设计清晰度的补充要求

  - [x] 8.3 落地关键页面 UI 线框级结构
    - 新手首页线框：顶部品牌与当前阶段、三步起步任务、今日待办、核心指标、推荐下一步、模块入口矩阵
    - 监测对象页线框：顶部解释与主按钮、搜索筛选工具栏、指标摘要、对象列表、行操作、右侧详情抽屉
    - 内容运营页线框：内容任务列表、模板筛选、发文统计摘要、左配置右结果创作台、发布检查面板
    - 发布运营页线框：平台统计卡、账号状态、渠道规则、内容发布列表、复测状态、平台明细抽屉
    - 分析诊断页线框：顶部诊断结论、平台/对象筛选、趋势区、明细表、建议任务区
    - 覆盖需求 1.1、3.1、5.1、6.3、7.1、12.4；对应用户补充“详细、精细、全面的 UI 重新优化”

  - [x] 8.4 执行公开 UI 用词和内部字段治理
    - 扫描前端公开文案，移除 demo、mock、内部 code、拼音平台名、内部 ID、伪链接和工程化状态文本
    - 将状态枚举转为业务状态，将平台 code 转为业务平台名，将技术字段收纳到详情抽屉或隐藏
    - 覆盖需求 12.1-12.5；对应正确性属性 5、6

  - [x]* 8.5 编写全局 UI 治理测试
    - 增加关键页面文案测试，覆盖平台显示、空态引导、状态标签和内部字段隐藏
    - 覆盖需求 12.1-12.5；对应 Web 测试 `工作台模块入口展示`、`品牌资料库缺失项引导`

  - [x] 8.6 精简全平台公开文案
    - 统一页面标题、功能说明、按钮、状态、空状态和错误提示的表达方式
    - 保留优化单元、用户意图、AI 回复监测等既有业务术语，清理公开页面中的 code、ID、Prompt、provider、fallback 和实现细节
    - 补充共享文案测试，覆盖需求 12.6-12.9

  - [x] 8.7 依据参考产品统一业务板块命名
    - 将一级导航更新为工作台、品牌信息、内容中心、发布中心和数据分析
    - 统一数据总览、营销画布、品牌信息、竞品信息、AI 平台管理、优化建议和发布记录的导航入口与页面标题
    - 保留 AI 回复监测、内容资产、顾问服务和再次监测等项目特有能力名称，并保持现有路由及工作流参数
    - 更新导航测试和项目架构文档，覆盖需求 13.1-13.5

- [x] 9. 检查点 - 确保所有测试通过
  - 确保所有测试通过,如有疑问请询问用户

- [x] 10. 补齐后端持久化、品牌隔离和安全边界
  - [x] 10.1 扩展 shared-types 和 repository port
    - 为品牌资料库、媒体资产、内容资产、媒体账号、平台规则、发文统计、分析 finding 和页面聚合 dashboard 增加共享类型
    - 在 `PermissionsRepositoryPort` 或对应模块 repository port 中补齐读取、创建、更新和聚合查询方法
    - 所有新增业务记录保留 `brandId` 并复用现有品牌访问策略
    - 覆盖需求 2.1、5.5、6.1、7.4、8.4、9.4、10.3；对应 beginner-friendly 属性 B1 和设计正确性属性 3、4

  - [x] 10.2 定义后端页面聚合 DTO 和字段映射
    - 定义 `BeginnerHomeDashboard`，聚合品牌资料完整度、监测对象数量、真实回复状态、内容任务、发布统计、分析风险和下一步动作
    - 定义 `MonitoringObjectDashboard`，聚合优化单元、用户意图、监测问题、推荐度、平均排名、引用率、内容任务和复测状态
    - 定义 `ContentOperationDashboard`，聚合内容任务、模板、素材、发布准备、发布统计和再次监测状态
    - 定义 `PublishingOperationDashboard`，聚合媒体账号、平台规则、内容发布记录、AI 引用、发布后表现和待复测事项
    - 定义 `AnalysisDiagnosisDashboard`，聚合竞品、评价、信源、事实四类 finding 和可执行建议
    - 覆盖需求 1.1、3.2、5.1、6.3、7.1、8.1、9.1、10.1；对应设计 `Backend Interfaces`

  - [x] 10.3 实现 memory repository 聚合数据路径
    - 在内存仓储中实现 profile-library、owned-media、content-assets、analysis dashboards 和 publishing stats 的聚合读取与写入
    - 补充 demo 数据，使新手首页、内容运营、媒体发布统计和分析诊断能在无数据库环境展示完整路径
    - 覆盖需求 1.1、2.1、5.1、6.1、7.1、8.1、9.1、10.1

  - [x] 10.4 实现 Prisma repository 聚合数据路径
    - 基于现有 Prisma 模型或轻量新增模型补齐资料库、媒体资产、发布统计和分析 finding 的持久化读取
    - 如需新增 Prisma 模型，创建迁移并保持 `npm run prisma:validate` 和 `npm run prisma:generate` 通过
    - 覆盖需求 2.5、5.5、6.2、6.4、7.4、10.3；对应设计 `Data Models`

  - [x] 10.5 实现 BFF 聚合控制器和服务层
    - 在品牌、内容、发布或新增 dashboard 模块中提供页面聚合 API，保持统一 `ApiResponse<T>` 响应结构
    - 控制器通过 `x-brand-id` 和现有品牌访问 middleware 校验品牌访问权限
    - 服务层只做聚合、映射、状态翻译和下一步动作生成，底层 CRUD 继续复用现有模块仓储
    - 聚合接口失败时返回用户可理解错误码和 fallback 空态所需字段
    - 覆盖需求 1.1、2.6、4.5、5.1、6.3、7.1、12.4；对应架构文档 API 响应约定

  - [x] 10.6 强化公开响应脱敏和真实指标边界
    - 检查平台配置、浏览器会话、LLM 任务、自动化任务包、Sprint 聚合和新增页面聚合接口的响应字段
    - 公开响应只返回脱敏状态、状态摘要、关联 ID、业务标签和用户可理解原因
    - 指标聚合层继续只读取真实 AI 回复、浏览器辅助结果或手动录入真实回复
    - 覆盖需求 4.5、11.3、11.4、12.5；对应正确性属性 1、2、4

  - [x]* 10.7 编写后端 API、repository 和安全属性测试
    - 覆盖新增聚合接口的品牌隔离、memory/Prisma 一致性、公开响应脱敏和缺失数据 fallback
    - Property P6：任意新增业务记录必须包含 `brandId` 并通过品牌访问校验，覆盖 beginner-friendly 属性 B1
    - Property P7：任意公开平台配置响应不得包含真实密钥、cookies、storage state 或 browser profile 路径，覆盖需求 11.3 和正确性属性 4

- [x] 11. 完成端到端页面串联和回归验证
  - [x] 11.1 串联新手路径的页面跳转和动作链
    - 确保“补资料 -> 建优化单元 -> 建用户意图 -> 开始监测 -> 看诊断 -> 生成内容 -> 发媒体 -> 再次监测”每一步都有可见入口
    - 关键行操作统一跳到对应页面并携带可复用上下文参数或 hash 锚点
    - 覆盖需求 1.5、3.5、4.1、5.5、6.4、7.4、10.3

  - [x] 11.2 更新路由、懒加载和预览配置
    - 检查 `routePaths.ts`、`routes.tsx`、`WorkspaceRouteRedirect.tsx` 和导航别名，确保新增或复用页面都能访问
    - 保持 Vite `/api` 代理和 `.monkeycode-ai.online` allowedHosts 配置
    - 覆盖需求 1.2、12.4；对应架构文档前端路由约定

  - [x] 11.3 执行交付验证命令
    - 在 `当前工作区` 运行 `npm run verify`
    - 运行 `git diff --check`
    - 如 Prisma schema 有变更，确认 `npm run prisma:validate` 和 `npm run prisma:generate` 在 verify 中通过
    - 覆盖设计 `Verification Commands`

  - [x]* 11.4 编写自动化端到端测试
    - 覆盖新手首轮路径：新手首页、品牌资料、优化单元、用户意图、AI 回复监测、内容生成、发布准备和再次监测入口
    - 覆盖分析异常到内容任务、事实修正和复测入口
    - 覆盖需求 1.1、1.5、4.1、5.5、6.4、7.4、10.3

- [x] 12. 检查点 - 确保所有测试通过
  - 确保所有测试通过,如有疑问请询问用户

- [x] 13. 修复品牌资源权限矩阵和前端能力一致性
  - [x] 13.1 重构品牌嵌套路由权限解析
    - 修改 `brand-access.policy.ts`，按标准化请求方法和具体业务路由模板优先匹配监测、内容、发布、任务、再次监测、报告和品牌主体资源
    - 将 viewer、analyst、operator、admin 和 owner 的资源动作矩阵定义为共享后端策略，避免 `/api/v1/brands` 前缀覆盖具体嵌套资源
    - 覆盖需求 14.1、14.2、14.4；对应设计 `Resource-Aware Authorization`

  - [x] 13.2 对齐前端能力摘要与权限错误体验
    - 由公开权限摘要驱动监测、内容、发布、任务和报告按钮状态
    - 权限不足时展示目标资源、当前角色、所需角色和申请路径，并保留用户已填写输入
    - 覆盖需求 14.3、14.5；对应正确性属性 7

  - [x] 13.3 编写权限矩阵 API、Web 和属性测试
    - 遍历全部角色、资源、方法和品牌嵌套路由，验证 operator 可执行监测、内容、发布、任务和再次监测动作
    - Property P8：任意公开能力摘要与服务端授权结果必须一致，覆盖需求 14.1-14.5 和正确性属性 7
    - 覆盖同组织、跨组织、无品牌上下文和品牌主体管理边界

- [x] 14. 建立真实再次监测和证据验收链路
  - [x] 14.1 重构再次监测领域状态和执行入口
    - 安排任务时只保存 `sourceRunId`、原问题、目标平台和计划时间，执行时创建新的 `MonitoringRun` 并回写 `retestRunId`
    - 将状态统一为 planned、collecting、analyzing、improved、unchanged 和 regressed，历史证据不足记录保持可读
    - 覆盖需求 17.1、17.2、17.5、17.6；对应设计 `Evidence-Based Retest`

  - [x] 14.2 从真实分析派生验收结果和实际分数
    - 使用再次监测原始回答和分析结果计算 `actualScore`、提及率、排名、事实准确度和引用指标变化
    - 保留人工输入用于目标值、备注和证据确认，禁止人工分数直接完成任务
    - 覆盖需求 17.3、17.4；对应正确性属性 8、9

  - [x] 14.3 改造再次监测页面为采集和分析状态流
    - 修改 `TaskRetestPage.tsx`，展示原问题、基线运行、新运行、采集状态、分析状态、优化动作和验收结论
    - 提供同题监测、分析重试、证据查看和继续优化入口
    - 覆盖需求 17.1-17.6；对应 Web 测试 `再次监测采集、分析和验收状态`

  - [x] 14.4 编写再次监测 API、Web 和属性测试
    - 覆盖 API、浏览器辅助、手动真实回复、采集失败、分析失败、无改善和退化路径
    - Property P9：任意已完成再次监测的两个运行标识必须属于同一品牌且互不相同，覆盖正确性属性 8
    - Property P10：任意改善结论必须同时关联基线分析、再次监测分析和可追溯优化动作，覆盖正确性属性 9

- [x] 15. 实现周期报告快照和效果证据
  - [x] 15.1 建立统一报告周期查询边界
    - 实现 `PeriodReportSnapshotService`，将起止日期转换为一致的 UTC 包含边界
    - 在 memory 和 Prisma repository 中按周期过滤监测、内容、发布、任务状态变化和已完成再次监测
    - 覆盖需求 18.1、18.2；对应设计 `Period-Scoped Reports`

  - [x] 15.2 冻结报告快照、口径和数据缺口
    - 报告生成前返回范围预览、有效样本数和缺口，生成后冻结 snapshot、样本摘要、口径版本和优化动作关系
    - 单品牌与多品牌报告使用相同周期和有效样本规则
    - 覆盖需求 18.3-18.6、21.5；对应正确性属性 9、10

  - [x] 15.3 升级报告中心的范围预览和证据下钻
    - 修改 `ReportCenterPage.tsx`，在生成前展示统计周期、数据类型计数、有效样本和数据缺口
    - 在效果变化中展示基线、观察窗口、优化任务、内容资产、发布渠道、真实链接和再次监测证据
    - 覆盖需求 18.3-18.6；对应设计 `EffectEvidencePanel` 和 `ReportScopePreview`

  - [x] 15.4 编写周期报告 API、Web 和属性测试
    - 覆盖跨日、月末、闰日、时区边界、空周期、多品牌与历史快照稳定性
    - Property P11：任意报告快照业务记录的时间必须处于报告周期内，覆盖正确性属性 10
    - 验证后续业务数据变化不会改变已冻结报告内容

- [x] 16. 升级行动型品牌项目主页
  - [x] 16.1 实现服务端主行动和前三待办排序
    - 基于数据阻断、人工确认、待执行任务、到期时间和业务价值生成 `BrandActionDashboard`
    - 复用现有 `BeginnerHomeDashboard`、Sprint 和工作流上下文，输出本周期效果摘要与证据状态
    - 覆盖需求 16.1、16.2、16.4、16.5；对应设计 `Action-Oriented Project Home`

  - [x] 16.2 改造主页首屏和行动反馈
    - 修改 `BrandWorkspacePage.tsx` 和 `BeginnerHomePanel.tsx`，首屏只突出一个主行动、前三项待办和本周期效果
    - 完成动作后刷新聚合状态，深链继续保留品牌、优化单元、用户意图、问题和来源运行
    - 覆盖需求 16.1-16.5；对应正确性属性 5、6

  - [x] 16.3 编写主页排序、状态和响应式测试
    - 覆盖资料缺失、待确认、待监测、待发布、待复测、全部完成和部分接口失败状态
    - 验证桌面与移动端的单一主行动、前三待办和上下文深链

- [x] 17. 检查点 - 完成可信度版本
  - [x] 17.1 执行权限、再次监测、报告和主页相关测试
    - 确保所有测试通过,如有疑问请询问用户
  - [x] 17.2 执行 `npm run verify` 和 `git diff --check`
    - 修复全部回归后再进入快速闭环阶段

- [x] 18. 建立官网快速接入和首次有效洞察
  - [x] 18.1 实现可恢复的四步接入会话
    - 新增 `QuickStartWizard` 与服务端草稿，按官网信息、事实确认、问题选择和执行准备分步保存
    - 从官网抓取结果生成带来源和置信状态的候选事实，关键事实经用户确认后进入标准答案和内容生成
    - 覆盖需求 15.1-15.3、15.6；对应正确性属性 11

  - [x] 18.2 生成六个默认高价值问题和执行准备摘要
    - 默认覆盖品牌、品类、地域、购买决策、竞品比较和用户痛点，并允许编辑与查看更多
    - 展示目标平台、连接状态、预计样本数、执行方式和下一步，直接进入首轮监测
    - 覆盖需求 15.4、15.5；对应设计 `Quick Diagnosis`

  - [x] 18.3 编写快速接入 API、Web 和属性测试
    - 覆盖分步保存、离开恢复、抓取失败、候选事实确认、资料缺失和首轮监测跳转
    - Property P12：任意候选事实必须保留来源和确认状态，覆盖正确性属性 11

  - [x] 18.4 建立官网来源页面计划和范围确认
    - 在深度抓取前展示 URL、标题、首页或产品等资料角色、选取原因和处理状态，并允许用户增删同源页面
    - 确定性降级仍需生成可解释候选，部分页面失败时保留成功结果和定向重试
    - 覆盖需求 34.1、34.2；对应 `SourcePagePlanEditor`

  - [x] 18.5 编写来源页面计划测试
    - 覆盖同源过滤、重复规范化、页面角色、人工增删、确定性降级和部分抓取失败
    - 验证未确认页面不会进入深度抓取和品牌知识片段

- [x] 19. 建立站点审计、任务验收和技术资产
  - [x] 19.1 实现受控站点审计 Adapter
    - 检查 robots.txt、sitemap.xml、llms.txt、noindex、AI Bot 访问、JSON-LD 和内容抽取结构
    - 仅访问用户提交的公开 HTTP(S) 官网与同源资源，限制重定向、响应大小和执行时间，并保存访问证据
    - 覆盖需求 19.1、19.2、19.5；对应设计 `Site Audit And Technical Assets`

  - [x] 19.2 将审计问题映射为任务和 checker 规则
    - 每个问题输出 pass、warning、fail 或 unavailable、影响级别、修复说明、任务模板和验收规则
    - 任务复查时重新获取目标并执行结构、文本、链接或响应头 checker，回写证据和验收结果
    - 覆盖需求 19.2、19.4；吸收 GeoLook 工单与自动验收机制

  - [x] 19.3 生成可部署技术资产
    - 基于已确认品牌事实生成 llms.txt、Organization JSON-LD、FAQPage JSON-LD、Article JSON-LD、FAQ 内容块和部署说明
    - 将结果保存为带目标页面、来源事实、审核状态和版本的内容资产
    - 覆盖需求 19.3、23.1-23.3；对应 `TechnicalAssetService`

  - [x] 19.4 建设站点审计工作台
    - 实现 `SiteAuditWorkbench`，展示检查状态、原始证据、影响、修复任务、资产生成和再次验收
    - 访问失败时保留已完成结果并提供定向重试入口
    - 覆盖需求 19.1-19.5

  - [x] 19.5 编写站点审计、checker 和资产测试
    - 覆盖各检查规则、同源边界、重定向限制、超时、异常文档和部分成功状态
    - 验证任务验收只依据真实目标复查结果，生成资产只使用已确认事实

  - [x] 19.6 实现版本化诊断评分
    - 保存 schema、meta、content、citation 等维度的原始检查、维度分数、归一权重、规则版本和综合分
    - 规则更新只影响新诊断，历史报告使用冻结版本复现原结果
    - 覆盖需求 32.1、32.2；对应 `DiagnosticScorePolicyService`

  - [x] 19.7 建立验收进度快照、历史证据和回归重开
    - 保存首次进度、当前进度、目标值和每次 checker 证据
    - 已验收任务再次检查失败时重新进入待处理，同时保留历史通过记录
    - 覆盖需求 32.3-32.5；对应 `AcceptanceHistoryService`

  - [x] 19.8 编写评分版本和回归验收属性测试
    - Property P18：任意历史诊断必须使用冻结评分规则版本复现，覆盖正确性属性 22
    - Property P19：任意 checker 回归失败的已验收任务必须重新进入待处理，覆盖正确性属性 23
    - 覆盖权重非法、规则升级、重复验收、不可判定和通过后回归

- [x] 20. 建立样本回放、测量基线和机会地图
  - [x] 20.1 扩展样本测量条件和证据等级
    - 为监测运行和样本保存平台、模型、采集方式、联网状态、市场、语言、证据等级、人工确认和基线版本
    - 为历史数据提供明确的未知测量条件状态，避免伪造默认值
    - 覆盖需求 21.1-21.3、21.6；对应 `MeasurementScope`

  - [x] 20.2 实现指标到原始样本的证据查询
    - 新增 `SampleEvidenceService` 和样本证据 API，返回问题、原始回答、引用来源、分析结果、测量条件和采集时间
    - 在指标、趋势、分析 finding 和任务中接入 `SampleEvidencePanel`
    - 覆盖需求 21.1、21.2、21.4

  - [x] 20.3 建立可比基线和观察归因
    - 测量条件变化时创建新 `baselineVersion`，趋势按可比区间分段
    - 区分未测、样本不足和有效样本，展示提及率、Top 3、事实准确度、引用召回与引用准确度
    - 保存基线窗口、观察窗口、对照问题和外部事件，默认输出观察相关结论
    - 覆盖需求 21.2-21.5；对应正确性属性 12、13

  - [x] 20.4 实现八维问题拓展和确定性降级
    - 基于品牌资料与种子词从品牌、品类、场景、人群、痛点、地域、购买决策和竞品比较生成候选
    - 合并重复候选并返回业务价值、推荐概率、用户阶段、目标平台、生成依据和生成方式
    - AI 服务失败时返回确定性候选并保留编辑入口
    - 覆盖需求 22.1、22.2、22.6；吸收 GEORank 八维拓词机制

  - [x] 20.5 实现竞品主题和真实信源渠道地图
    - 从有效 AI 回复提取竞品优势主题、引用域名、来源类型、平台分布和引用位置
    - 按品牌缺席、竞品占优、内容缺失和事实不一致排序内容机会，渠道建议优先使用真实样本来源
    - 覆盖需求 22.3-22.5；吸收 GeoLook 竞品引用与渠道地图机制

  - [x] 20.6 编写测量、问题拓展和机会地图测试
    - Property P13：任意指标样本必须关联测量条件、证据等级和原始回答，覆盖正确性属性 12
    - Property P14：任意跨周期趋势只比较同一基线版本的兼容测量条件，覆盖正确性属性 13
    - 覆盖八维生成、去重、AI 降级、真实信源排序和样本稀疏状态

  - [x] 20.7 隔离品牌探测题与跨访问端指标
    - 按品牌规范名、别名和官网域名识别 `brand_probe`，无提示提及、首位和 Top 3 指标只使用 discovery 问题
    - 品牌探测独立展示识别率、事实准确度和自有域名引用率；样本按市场、API/Web/App 访问端和采集条件分序列
    - 覆盖需求 28.1-28.4；对应 `PromptMeasurementBreakdown`

  - [x] 20.8 实现未测权重归一、比较门禁和趋势状态
    - 复合指标只对已测子项重新归一权重，验收未测时进入待补测
    - 同市场至少两个有效且存在差异的平台结果才能生成强弱比较
    - 单期变化保持观察，三个连续可比快照中的两次同向变化升级为趋势，基线变化重置计数
    - 覆盖需求 28.5-28.10；对应 `MetricIntegrityService`

  - [x] 20.9 建立竞品候选证据生命周期和问题机会判定
    - 增加候选、样本确认、用户确认和排除状态，分析、内容及报告只消费已确认竞品
    - 识别竞品失守、品牌独占和竞品最强三个可比平台，并接入内容任务创建
    - 覆盖需求 29.1-29.6；对应 `CompetitorCandidate`

  - [x] 20.10 接入搜索补全需求快照和人工入库
    - 通过 Adapter 保存词根、来源、市场、采集时间和候选问句，确认后加入稳定监测问题库
    - 对上一快照未出现的候选词标记需求上升观察，保持历史问题口径不变
    - 覆盖需求 29.7-29.9；对应 `DemandSnapshotService`

  - [x] 20.11 生成渠道建设蓝图和 30/60/90 路线图
    - 将真实信源机会映射为内容形态、建议数量、节奏、负责角色、优先级和依据
    - 按 0 至 30 天、30 至 60 天和 60 至 90 天安排动作，并按真实引用域名更新渠道覆盖
    - 覆盖需求 30.1-30.3；对应 `ChannelRoadmapService`

  - [x] 20.12 编写高级测量、竞品、需求和渠道属性测试
    - Property P20：任意无提示发现指标必须排除品牌探测题，覆盖正确性属性 17
    - Property P21：任意复合指标必须在排除未测项后将权重归一为 1，覆盖正确性属性 18
    - Property P22：任意趋势结论必须由三个连续可比快照中的两次同向变化支持，覆盖正确性属性 19
    - Property P23：任意竞品分析、内容或报告只能关联已确认竞品，覆盖正确性属性 20
    - 覆盖别名与域名分类、单平台、全相同、方向反转、搜索来源失败、候选确认和渠道域名匹配

- [x] 21. 强化内容质量、品牌知识检索和效果归因
  - [x] 21.1 实现内容发布准备检查服务
    - 检查定义块、FAQ、步骤、比较表、数字依据、作者、更新时间、外部引用、结构化数据和渠道格式
    - 将品牌事实、数字和高风险段落映射到来源、核验日期、待确认状态和修正入口
    - 覆盖需求 23.1-23.4；对应 `ContentReadinessService`

  - [x] 21.2 强化发布确认和真实结果记录
    - 创建发布任务前确认账号、内容版本、发布方式、素材要求和再次监测计划
    - 发布完成后保存真实链接、时间、版本和渠道结果，并接入复测与效果证据
    - 覆盖需求 23.4-23.6、18.4

  - [x] 21.3 建立品牌资料片段和来源版本
    - 将上传资料、官网抓取和已确认事实转换为 `KnowledgeChunk`，保存品牌、来源、版本、内容哈希、审核状态和更新时间
    - 首期使用 PostgreSQL 全文检索和现有资料模型实现，保留向量与关系检索 Adapter 边界
    - 覆盖需求 25.1、25.5；对应设计 `Knowledge Retrieval`

  - [x] 21.4 实现带证据问答和业务调用留痕
    - 知识查询返回答案、引用片段、来源、更新时间和可信状态
    - 内容生成与事实分析保存使用的资料片段，缺少确认依据时返回资料缺口
    - 覆盖需求 25.2-25.4

  - [x] 21.5 建立优化动作到效果证据的聚合
    - 聚合基线运行、优化任务、内容资产、发布记录、真实链接、再次监测和报告周期
    - 在主页、分析、任务和报告页面复用 `EffectEvidencePanel`，显示完整证据或缺口
    - 覆盖需求 18.3-18.6、20.2

  - [x] 21.6 编写内容准备、知识检索和归因测试
    - Property P15：任意内容事实或数字必须关联来源或待确认状态，覆盖正确性属性 14
    - 覆盖来源版本、品牌隔离、资料更新、检索缺口、发布确认和完整归因链

  - [x] 21.7 实现引用广度与答案吸收深度证据
    - 将回答句与引用页面片段进行 supports、partial、conflicts 和 unavailable 匹配
    - 保存支持范围、置信度和复核状态，在信源分析中分别展示引用广度与吸收深度
    - 覆盖需求 30.4-30.6；对应 `CitationAbsorptionService`

  - [x] 21.8 增加对比、榜单和 FAQ 专属内容门禁
    - 对比内容检查同口径维度、自身局限和核验日期，榜单检查评选方法、数据来源和利益披露
    - FAQ 检查每个答案首句的直接结论，并为失败规则提供结构化修正入口
    - 覆盖需求 31.1-31.3；对应版本化内容规则集

  - [x] 21.9 强化检索连续性和 BYOK 成本边界
    - 向量或关系检索不可用或结果不足时，使用审核后的全文与结构化结果补齐并返回 `retrievalMode`
    - BYOK 场景显式选择组织 Embedding、平台额度或全文降级，结果继续展示来源、审核和可信状态
    - 覆盖需求 34.3-34.5；对应 `KnowledgeRetrievalService`

  - [x] 21.10 编写引用吸收、专属内容和检索连续性测试
    - Property P24：任意引用吸收结论必须关联回答句、引用片段和复核状态，覆盖正确性属性 21
    - 覆盖准确、部分、冲突、来源不可访问、缺比较维度、缺披露、零向量、部分召回、Embedding 故障和 BYOK

- [x] 22. 建立产品关键步骤事件和效果看板
  - [x] 22.1 实现隔离的产品事件采集
    - 记录品牌创建、资料确认、首轮监测、建议采纳、内容保存、发布、再次监测、报告生成和失败类别
    - 事件按组织和品牌隔离，避免记录凭据、原始私密资料和内容正文
    - 覆盖需求 20.1、20.3、20.4

  - [x] 22.2 实现产品效果聚合与运营看板
    - 计算首轮监测到达率、首次有效洞察耗时、建议采纳率、发布完成率、再次监测完成率和有改善任务占比
    - 展示统计周期、样本规模、分母定义和数据缺口
    - 覆盖需求 20.2、20.4

  - [x] 22.3 编写事件和效果聚合测试
    - 覆盖事件幂等、组织与品牌隔离、失败分类、周期过滤和空样本
    - 验证首次有效洞察耗时从接入开始到首个有效样本分析完成

- [x] 23. 检查点 - 完成快速闭环与执行交付版本
  - [x] 23.1 执行快速接入、站点审计、样本证据、机会、内容和知识相关测试
    - 确保所有测试通过,如有疑问请询问用户
  - [x] 23.2 执行 `npm run verify` 和 `git diff --check`
    - 修复全部回归后再进入规模化运营阶段

- [x] 24. 建立真实发布 Adapter 注册中心
  - [x] 24.1 定义统一发布 Adapter 契约和能力摘要
    - 统一连接校验、草稿创建、发布执行、状态查询、真实链接、错误分类和幂等键
    - 公开响应只返回脱敏连接状态、能力和恢复动作
    - 覆盖需求 6.1-6.5、11.3、23.5、23.6

  - [x] 24.2 实现 WordPress、GitHub Contents、微信公众号和通用 Webhook Adapter
    - 各 Adapter 映射内容格式、素材、账号和发布结果，并复用现有发布记录状态机
    - 自动化能力受平台接口限制时进入半自动确认状态并保留真实链接录入
    - 覆盖需求 6.2-6.5、23.4-23.6；吸收 GeoLook 发布 Adapter 机制

  - [x] 24.3 编写发布 Adapter 契约和集成测试
    - 使用本地模拟服务覆盖成功、鉴权失效、限流、超时、重复提交、部分素材失败和状态恢复
    - 验证凭据脱敏、品牌隔离、幂等和真实链接持久化

  - [x] 24.4 增加执行时发布确认和渠道草稿语义
    - 每次执行展示账号、内容版本、目标渠道和结果类型并要求本次确认
    - Adapter 能力摘要增加 `resultMode`，WordPress 与微信公众号固定记录为待后台确认草稿
    - 覆盖需求 31.4、31.5；吸收 GeoLook 的显式发布确认机制

  - [x] 24.5 编写发布确认与草稿状态测试
    - 覆盖确认取消、内容版本变化、账号失效、草稿创建、直接发布和人工交接
    - 验证未经本次确认的请求不会调用渠道 Adapter

- [x] 25. 建设 Provider、额度和持久异步任务治理
  - [x] 25.1 实现 Provider 池与组织 BYOK 配置
    - 建立 Provider 用途、模型、健康状态、优先级、故障转移顺序和服务端凭据引用
    - 组织自有配置按组织隔离，调用审计记录实际 Provider 和尝试顺序
    - 覆盖需求 26.1、26.2、26.5；对应 `ProviderGovernanceService`

  - [x] 25.2 实现额度预占、结算和预算边界
    - 在 AI 任务接受前校验用户、组织和全局预算，创建预占记录
    - 成功按真实使用结算，失败或取消释放，重试和故障转移归入同一任务账本
    - 覆盖需求 26.3；对应正确性属性 15

  - [x] 25.3 实现持久化任务编排器
    - 保存任务类型、队列状态、步骤进度、幂等键、重试次数、错误类别和最终结果
    - 将抓取、监测、内容生成、发布、复测、报告和交付逐步迁移到统一任务边界
    - 覆盖需求 24.2、26.4、26.5；对应 `JobOrchestrator`

  - [x] 25.4 建设管理员运行中心
    - 展示 Provider 健康、队列积压、失败任务、额度使用、发布账号和依赖服务状态
    - 提供按权限控制的定向重试、取消和查看尝试记录入口
    - 覆盖需求 26.6

  - [x] 25.5 编写 Provider、额度、任务和运行中心测试
    - Property P16：任意 Provider 使用结算必须对应同一任务的一次预占、释放或完成结算，覆盖正确性属性 15
    - 覆盖 BYOK 隔离、优先级、故障转移、重试上限、幂等、任务恢复和公开响应脱敏

  - [x] 25.6 实现 Provider 主动健康测试和启用门禁
    - 执行受限最小请求并保存成功状态、响应状态、延迟、测试时间、脱敏错误类别和有效期
    - 启用与路由决策展示最近健康结果，过期配置进入待复测状态
    - 覆盖需求 33.1、33.2

  - [x] 25.7 建立稳定额度拒绝原因和人工调整审计
    - 区分用户冻结、用户耗尽、组织耗尽、全局预算耗尽和 BYOK 强制模式，保留任务输入与恢复动作
    - 管理员调整额度必须填写原因并保存操作者、主体、前后状态、差值和时间，审计记录只追加
    - 覆盖需求 33.3-33.5；对应 `QuotaAdjustmentService`

  - [x] 25.8 实现 Provider 成本步骤租约和追加式成本
    - 按任务、步骤和尝试获取有限期租约，有效租约内重复投递执行幂等跳过
    - Provider 返回后立即追加已发生成本，Worker 崩溃、租约过期和后续失败保留已发生成本
    - 覆盖需求 33.6-33.8；对应 `ProviderSpendStage`

  - [x] 25.9 实现失败任务原子重试与派发补偿
    - 状态比较交换中完成旧预占结清、新尝试和新预占创建
    - 状态已变化或队列派发失败时阻止重复运行并释放新增预占
    - 覆盖需求 33.9、33.10

  - [x] 25.10 编写 Provider 事务和额度审计属性测试
    - Property P25：任意人工额度调整必须生成含原因和前后状态的只追加审计，覆盖正确性属性 24
    - Property P26：任意产生成本的步骤在有效租约内最多调用一次 Provider，覆盖正确性属性 25
    - 覆盖连接成功、鉴权失败、模型不存在、超时、并发领取、租约过期、重复完成、双击重试、派发失败和并发额度调整

- [x] 26. 建立周期运营、客户空间和完整交付包
  - [x] 26.1 实现周期运营编排
    - 新增 `OperationCycleService`，按配置串联抓取、审计、监测、任务验收、报告和交付包步骤
    - 展示当前步骤、进度、失败原因、重试状态和人工确认事项，支持中断后恢复
    - 覆盖需求 24.1、24.2；对应 `OperationCycle`

  - [x] 26.2 实现冻结交付包与格式导出
    - 从冻结报告快照生成诊断、指标明细、任务、内容与技术资产、发布记录、复测证据和口径说明
    - 输出 HTML、PDF、Markdown 和 CSV，并保存文件 manifest、品牌、周期、口径版本和生成时间
    - 覆盖需求 24.3、24.4、24.6；对应正确性属性 16

  - [x] 26.3 建设客户只读空间和跨品牌比较
    - 使用独立只读授权展示指定品牌的报告、任务进度、发布证据、效果变化和数据缺口
    - 多品牌比较只使用相同统计周期、指标口径和可比基线版本
    - 覆盖需求 24.5、18.5、21.3

  - [x] 26.4 编写周期、交付包和客户空间测试
    - Property P17：任意交付包必须引用冻结快照并具有包含全部成功文件的 manifest，覆盖正确性属性 16
    - 覆盖周期恢复、部分文件失败、格式导出、只读权限、品牌隔离和跨品牌可比性

  - [x] 26.5 实现站点审计与答案采样的独立周期
    - 默认每周执行站点审计、每两周执行 AI 回答采样，支持双周和月度采样模板
    - 调整采样频率前展示问题数、平台数、轮次、预计成本和趋势可解释性影响
    - 覆盖需求 35.1-35.3

  - [x] 26.6 拆分诊断报告、优化方案和执行方案
    - 诊断报告承载现状与证据，优化方案承载目标与机会，执行方案承载负责人、排期和验收标准
    - 三个文档引用同一冻结快照、口径版本和 manifest，并保持指标、任务和时间窗口一致
    - 覆盖需求 35.4、35.5

  - [x] 26.7 编写差异化周期和正式文档一致性测试
    - 覆盖默认频率、错峰执行、漏跑恢复、频率变更、成本预估和部分步骤失败
    - 验证三类文档的快照、指标、任务、路线图和版本引用一致

- [x] 27. 补齐场景指导、基础设施边界和公共品牌资产
  - [x] 27.1 扩展全局教程为上下文指导
    - 在关键步骤展示目标、完成标准、示例、下一步、指标口径、数据来源、适用边界和常见误读
    - 依据当前品牌状态推荐快速接入、监测、内容、发布、复测或高级分析入口
    - 覆盖需求 27.1、27.2、27.4；吸收 GEORank 教程体系的产品价值

  - [x] 27.2 建立错误类别到排查步骤的映射
    - 为平台连接、抓取、审计、监测、生成、发布、复测、报告、额度和交付错误提供场景化恢复步骤
    - 复用公开业务错误码，隐藏凭据、堆栈和内部基础设施信息
    - 覆盖需求 27.3

  - [x] 27.3 固化检索与存储基础设施触发门槛
    - 为知识检索、关系查询和对象资产记录容量、延迟与查询形态指标
    - 仅在设计定义的规模条件达到后通过 Adapter 接入 Qdrant、Neo4j 或对象存储，并保持业务 API 不变
    - 覆盖需求 25.1-25.5；对应设计 `Selective Adoption Boundaries`

  - [x] 27.4 编写指导、错误恢复和 Adapter 契约测试
    - 覆盖关键步骤上下文、术语解释、错误分类、品牌状态推荐和高级能力入口
    - 验证不同检索实现返回相同来源、版本、审核状态和可信状态契约

  - [x] 27.5 实现公开品牌档案审核、预览、发布和撤回
    - 审核预览授权绑定品牌和有效期并输出 `noindex`，正式发布输出稳定 canonical、Organization 与 WebPage JSON-LD
    - 公开页面只消费白名单内已确认资料，撤回后停止公开访问和新增浏览统计
    - 覆盖需求 36.1-36.4；对应 `PublicBrandProfileService`

  - [x] 27.6 编写公开品牌档案属性和路由测试
    - Property P27：任意公开品牌档案字段必须来自公开字段白名单和已确认资料，覆盖正确性属性 26
    - 覆盖预览授权范围、过期、`noindex`、发布、撤回、canonical、结构化数据和浏览统计边界

- [x] 28. 完成全链路回归与发布门禁
  - [x] 28.1 编写首轮闭环自动化端到端测试
    - 串联官网接入、事实确认、问题选择、真实监测、诊断、内容准备、发布记录、再次监测和周期报告
    - 覆盖浏览器辅助与手动真实回复路径、证据下钻和中断恢复
    - 覆盖需求 15.1-15.6、17.1-18.6、21.1-23.6

  - [x] 28.2 编写周期运营和客户交付自动化端到端测试
    - 串联周期创建、站点审计、任务验收、监测、报告、交付包和客户只读访问
    - 覆盖 Provider 故障转移、额度不足、任务重试和部分交付失败
    - 覆盖需求 19.1-36.4

  - [x] 28.3 更新项目架构和功能文档
    - 同步 `.monkeycode/docs/ARCHITECTURE.md`、API、数据模型、任务状态和验证命令
    - 记录渐进式基础设施触发条件与运行恢复流程

  - [x] 28.4 执行最终交付验证
    - 运行 `npm run verify`
    - 运行 `git diff --check`
    - 确认 API、Web、Prisma、构建和依赖审计全部通过

- [x] 29. 检查点 - 确保所有测试通过
  - [x] 29.1 完成 P0、P1、P2 全部必做任务和测试
    - 确保所有测试通过,如有疑问请询问用户
