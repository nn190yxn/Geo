# 需求实施计划

- [x] 1. 重构全局信息架构和新手入口
  - [x] 1.1 更新导航分组、路由命名和模块描述
    - 修改 `apps/web/src/layouts/navigation.ts`，将导航重组为“开始使用、监测对象、内容运营、发布运营、分析诊断”或等价的新手业务流程结构
    - 将 `品牌工作台` 调整为 `新手首页` 入口，将 `模型设置` 业务化为 `AI 平台`，将画布作为关系视图或高级视图入口
    - 保留现有路由能力和品牌上下文注入，避免破坏 `/brands/:brandId/*` 路由别名
    - 覆盖需求 1.1、1.2、12.1、12.4、12.5；对应设计 `Navigation Design` 和 `Page-Level Design/品牌工作台`

  - [x] 1.2 实现新手首页首屏任务工作台
    - 修改 `BrandWorkspacePage.tsx`，新增“从这里开始”区域，展示补品牌资料、创建监测对象、获取真实 AI 回复三步任务
    - 增加自然语言任务入口或问题式引导卡片，支持跳转到品牌资料、优化单元、用户意图、AI 回复监测和内容生成
    - 用当前 `BrandWorkspaceSnapshot`、`VisibilitySprint` 和现有计数生成待办状态、阶段进度和下一步动作
    - 覆盖需求 1.1、1.3、1.4、1.5；对应设计 `Page-Level Design/品牌工作台`

  - [x] 1.3 建立业务术语解释和显示标签封装
    - 新增或扩展 `apps/web/src/utils/displayLabels.ts`，集中维护优化单元、用户意图、信源分析、事实分析、内容引用、真实 AI 回复等解释文案
    - 在工作台、优化单元、用户意图、监测、内容和分析页面标题、副标题、空态和 tooltip 中复用业务解释
    - 确保 UI 展示豆包、Kimi、DeepSeek、通义千问、阶跃星辰等业务平台名
    - 覆盖需求 12.1、12.2、12.4、12.5；对应正确性属性 5、6

  - [x] 1.4 建立页面级 UI 设计骨架和组件清单
    - 在 `apps/web/src/styles/global.css` 中补齐新手首页、资产中心、对象列表、创作台、发布统计、分析诊断的页面级布局样式
    - 明确可复用 UI 组件形态：任务入口卡、下一步动作条、列表工具栏、筛选区、状态标签、诊断卡、平台统计卡、右侧详情面板、发布检查项
    - 为关键页面定义响应式约束：桌面端保持列表和详情并排，移动端按“标题说明 -> 主操作 -> 列表 -> 详情”顺序堆叠
    - 视觉上采用工作台型界面，优先使用紧凑列表、分组表单、工具栏和数据卡，控制装饰性卡片数量
    - 覆盖需求 1.1、1.2、5.1、6.3、7.1、12.4；对应用户补充“整体设计板块清晰、方便新手和企业工作人员使用”

  - [x]* 1.5 编写导航和术语显示测试
    - 更新 `navigation.test.ts`，覆盖新导航分组、关键路由、品牌化别名和工作流顺序
    - 为术语解释 helper 增加单元测试，覆盖平台名称、专业词解释和未知状态 fallback
    - 覆盖需求 1.2、12.1、12.2；对应正确性属性 5、6

- [x] 2. 建设品牌资产中心和资料完整度体验
  - [x] 2.1 扩展品牌资料库页面结构
    - 修改 `BrandKnowledgeCard.tsx` 和 `brandProfileLibrary.ts`，把品牌资料拆为基础信息、产品服务、目标用户、品牌知识、媒体资产、自有媒体账号、竞品信息七类
    - 每个分组展示完整度、缺失项、最近更新时间、可用于标准答案状态和补充入口
    - 对资料不足、上传失败、待确认字段使用可执行空态，引导用户编辑、上传或确认资料
    - 覆盖需求 2.1、2.5、2.6、12.3；对应设计 `Page-Level Design/品牌资料库`

  - [x] 2.2 设计品牌资产中心的分区交互和字段密度
    - 为基础信息区使用紧凑表单和右侧完整度摘要，字段覆盖品牌名称、官网、行业、地区、业务介绍、服务范围、联系方式和禁用表达
    - 为产品服务区使用可重复条目列表，字段覆盖产品名称、适用人群、使用场景、价格口径、优势、限制和 FAQ
    - 为目标用户区使用画像卡片列表，字段覆盖用户类型、决策阶段、关注问题、反对理由、常见搜索表达和高价值意图
    - 为知识库、图片素材、自有媒体和竞品信息使用列表页样式，提供搜索、类型筛选、状态筛选和主操作按钮
    - 覆盖需求 2.1-2.6、12.3；对应用户补充“板块清晰、企业人员能正常使用”

  - [x] 2.3 补齐品牌资料库前后端聚合接口
    - 在 shared-types 中定义 `BrandProfileLibrary`、`BrandProfileLibraryInput`、`BrandProfileLibrarySection` 和完整度摘要类型
    - 在 API 品牌模块增加 `GET /api/v1/brands/:brandId/profile-library` 与 `PATCH /api/v1/brands/:brandId/profile-library` 聚合接口，底层复用现有 `BrandProfile`、`KnowledgeSource`、`Competitor`、`PublishingAccount` 数据
    - 在 memory 和 Prisma repository 中实现聚合读取与增量保存，不改变真实密钥、cookies、storage state 的公开边界
    - 覆盖需求 2.1-2.6、11.3；对应设计 `Backend Interfaces` 和 `Data Models/BrandProfileLibrary`

  - [x] 2.4 增加图片素材和媒体资产最小模型
    - 在 shared-types 中定义媒体资产类型，字段包含素材类型、适用平台、适用内容、来源、审核状态和关联内容任务
    - 在前端品牌资料库中加入图片素材列表、上传占位入口和素材适用场景展示
    - 首版可复用 `KnowledgeSource` 或 `ContentAsset` 承接素材，不引入自动发布能力
    - 覆盖需求 2.1、2.5、5.2、6.4；对应设计 `Data Models/BrandProfileLibrary`

  - [x]* 2.5 编写品牌资料库单元和 API 测试
    - 为资料完整度、缺失项、分组进度和资料来源状态编写 Web helper 测试
    - 为 profile-library 聚合接口编写 API 测试，覆盖读取、更新、缺失字段和品牌隔离
    - 覆盖需求 2.1-2.6；对应测试策略 `品牌资料库读取和更新`

  - [x]* 2.6 编写资料完整度属性测试
    - Property P1：任意资料分组的完整度必须在 0 到 100 之间，覆盖需求 2.6 和设计 `Test Strategy/资料完整度计算`
    - Property P2：任意缺失字段必须能映射到补充入口或影响说明，覆盖需求 2.6 和错误处理 `资料缺失`

- [x] 3. 重构优化单元、用户意图和监测对象列表
  - [x] 3.1 改造优化单元列表为新手可执行对象页
    - 修改 `OptimizationUnitsCard.tsx`，增加页面说明：“优化单元是希望 AI 推荐的产品、服务或业务主题”
    - 增加搜索、类型筛选、优先级筛选、空态示例和新增按钮固定入口
    - 每行展示关联用户意图数、监测问题数、推荐度、平均排名、内容资产数和状态
    - 每行增加创建用户意图、开始监测、生成内容、查看分析、编辑等动作入口
    - 覆盖需求 3.1、3.2、4.1、5.1、12.3；对应设计 `GEO 画布` 和用户补充竞品逻辑

  - [x] 3.2 改造用户意图列表为真实问题管理页
    - 修改 `UserIntentPromptCard.tsx`，增加页面说明：“用户意图是客户可能向 AI 提出的真实问题”
    - 列表展示用户问题、关联优化单元、决策阶段、推荐度、平均排名、内容引用率、负面评价数、事实异常数、最近监测时间
    - 每行增加手动检测、自动监测、生成内容、查看检测记录、查看引用来源和编辑入口
    - 监测问题展开行增加无问题时的生成问题按钮和解释文案
    - 覆盖需求 3.3、4.1、4.6、5.1、9.2、10.2；对应设计 `MonitoringPlanWizard` 和 `GeoCanvasNodeDetails`

  - [x] 3.3 将关系视图作为对象列表后的高级视图
    - 修改 `GeoCanvasPage.tsx`，保留左侧优化单元列表、中间关系图、右侧详情面板
    - 页面标题和空态强调这是优化单元、用户意图、监测问题、平台表现、内容任务和复测结果的关系视图
    - 节点详情继续提供查看真实回复、生成内容任务、补充标准答案和再次监测入口
    - 覆盖需求 3.1-3.5；对应设计 `Page-Level Design/GEO 画布`

  - [x]* 3.4 编写监测对象 Web 测试
    - 覆盖优化单元空态、行操作、筛选项和新手解释展示
    - 覆盖用户意图行操作、监测问题展开空态和平台表现业务标签
    - 覆盖需求 3.1-3.5、4.1、12.3

- [x] 4. 重构 AI 回复监测创建、待补充队列和真实回复边界
  - [x] 4.1 实现监测计划创建向导 UI
    - 修改 `MonitoringPage.tsx`，新增“创建 AI 回复监测计划”向导，按优化单元、用户意图、监测问题、AI 平台、监测方式和频率组织
    - 监测方式显示自动监测、浏览器辅助监测、手动录入三类业务入口
    - 平台未配置时显示配置入口、手动录入入口和影响范围
    - 覆盖需求 4.1、4.2、4.3、11.5；对应设计 `Page-Level Design/AI 回复监测`

  - [x] 4.2 实现待补充真实回复队列
    - 在 `MonitoringPage.tsx` 中聚合缺少真实原始回复的监测项，展示问题、平台、缺少内容、推荐操作和处理入口
    - 保持 `mock_ai`、人工样例和标准答案不进入真实 AI 回复指标计算
    - 缺少真实回复时阻止该项进入指标计算，并引导浏览器辅助监测或手动录入
    - 覆盖需求 4.3、4.4、4.5、4.6；对应正确性属性 1、2

  - [x] 4.3 补齐监测运行公开状态和用户解释
    - 修改 `MonitoringRunsCard.tsx`、`ManualTestEntryCard.tsx`、`PlatformConfigCard.tsx` 和相关 helper
    - 将 API、浏览器、手动、失败、待确认、待补充状态转换成用户可理解的监测状态和下一步动作
    - 公开响应和前端展示只显示平台脱敏状态，不显示密钥、cookies、storage state 或 profile 路径
    - 覆盖需求 4.2-4.6、11.2-11.5、12.4；对应正确性属性 1、4、6

  - [x]* 4.4 编写真实回复边界单元和属性测试
    - Property P3：任意指标聚合只允许使用真实 API 回复、浏览器辅助结果或用户手动录入真实回复，覆盖需求 4.5 和正确性属性 1
    - Property P4：任意标准答案、内容草稿、mock 平台样例不得计入真实 AI 回复指标，覆盖需求 4.5 和正确性属性 2
    - 为待补充队列 helper 编写测试，覆盖自动监测、浏览器辅助和手动录入路径

- [x] 5. 建设内容生成、内容优化和内容资产运营链路
  - [x] 5.1 新增内容生成列表页结构
    - 修改或扩展 `ContentGenerationPage.tsx`，在创作台前增加内容任务列表视图或顶部列表区
    - 列表展示内容标题、内容模板、关联优化单元、关联用户意图、适用平台、状态、创建时间、发布统计和操作
    - 操作包含创建内容、继续编辑、发布准备、再次监测和查看统计
    - 覆盖需求 5.1、5.3、5.5、6.4；对应设计 `ContentStudio` 和用户补充三张媒体发文统计图

  - [x] 5.2 完善左配置右结果创作台 UI
    - 左侧配置包含内容目标、优化单元、用户意图、内容模板、发布平台、语气风格、引用资料、图片素材和合规要求
    - 右侧结果包含生成前空态、内容草稿、FAQ、引用依据、风险提醒、渠道发布检查和保存入口
    - 内容优化模式支持粘贴已有内容并输出结构建议、事实补强、FAQ 补充、引用补强和渠道适配建议
    - 覆盖需求 5.1-5.6、12.3；对应设计 `Page-Level Design/内容生成与内容优化`

  - [x] 5.3 细化内容创作 UI 状态和交互反馈
    - 创作台左侧配置按“目标与对象、模板与渠道、素材与依据、生成配置、发布检查”分组
    - 右侧结果区支持未生成、生成中、生成失败、待审核、已保存、已进入发布准备六类状态
    - 生成中状态展示涉及平台、当前步骤、预计下一步和保留任务后继续查看的提示
    - 发布检查区展示平台字数、封面图、素材、事实依据、引用依据、禁用表达和复测计划状态
    - 覆盖需求 1.3、5.1-5.6、6.4、12.4；对应用户补充三张内容创作和媒体发文图

  - [x] 5.4 建立内容模板和素材规则配置层
    - 在前端新增内容模板配置 helper，覆盖品牌介绍、问答科普、选购指南、竞品对比、案例故事、榜单推荐、本地攻略、FAQ 汇总、媒体稿、小红书种草、知乎长文和公众号文章
    - 为每类模板配置适用平台、推荐字数、素材要求、引用要求和复测建议
    - 模板选择结果写入现有内容生成任务输入或前端配置状态，不破坏后端内容生成契约
    - 覆盖需求 5.1、5.2、5.3、6.3；对应用户补充三张内容创作与媒体发布截图

  - [x] 5.5 补齐内容资产后端聚合接口
    - 在 shared-types 中扩展 `ContentAsset` 页面模型，包含优化单元、用户意图、sourceReferences、reviewStatus、publishStatus、retestPlanId 和发布统计摘要
    - 在内容模块或 BFF 聚合层提供 `GET /api/v1/brands/:brandId/content-assets` 与 `POST /api/v1/brands/:brandId/content-assets` 页面契约，复用现有内容资产、内容版本、发布记录和复测任务
    - 确保内容资产必须关联来源资料、优化单元或用户意图中的至少一类上下文
    - 覆盖需求 5.2、5.5、5.6；对应正确性属性 3

  - [x]* 5.6 编写内容链路测试和属性测试
    - 覆盖内容模板选择、素材规则、左右分栏空态、保存草稿和发布准备入口
    - Property P5：任意保存的内容资产必须至少关联来源资料、优化单元或用户意图中的一类上下文，覆盖需求 5.5 和正确性属性 3
    - 覆盖内容优化模式的事实补强、FAQ 补充和渠道适配提示

- [x] 6. 建设自有媒体、媒体平台和发文统计能力
  - [x] 6.1 重构发布中心为自有媒体和媒体平台双入口
    - 修改 `PublishingCenterPage.tsx`，按路由或页签区分自有媒体账号管理和媒体平台规则管理
    - 自有媒体展示官网、博客、公众号、知乎、小红书、B 站、视频号和其他账号配置状态
    - 媒体平台展示内容格式要求、适合用户意图、推荐发布频率、封面图比例和发布注意事项
    - 覆盖需求 6.1、6.2、6.3、12.4；对应设计 `Page-Level Design/自有媒体与媒体平台`

  - [x] 6.2 新增媒体账号和平台规则后端契约
    - 在 shared-types 中定义或扩展 `OwnedMediaAccount`、`MediaPlatformRule`、`PublishingChannelStats` 类型
    - 在发布模块增加 `GET /api/v1/brands/:brandId/owned-media`、`POST /api/v1/brands/:brandId/owned-media` 和平台规则聚合读取接口
    - memory 和 Prisma repository 复用 `PublishingAccount` 与 `PublishingRecord` 计算账号状态和发文统计
    - 覆盖需求 6.1-6.5；对应设计 `Backend Interfaces` 和 `Data Models/OwnedMediaAccount`

  - [x] 6.3 实现发文统计和渠道发布检查 UI
    - 在发布中心或内容生成列表中增加发文统计卡片，按公众号、小红书、知乎、B 站、官网、博客等平台展示发文数量、待发布数、已发布数、AI 引用次数、关联用户意图数和再次监测状态
    - 在内容发布准备中展示账号缺失、素材缺失、格式不匹配和待复测提醒
    - 覆盖需求 6.3、6.4、6.5、9.3；对应用户补充三张媒体发文统计图

  - [x] 6.4 设计媒体发布统计看板和平台明细页
    - 新增媒体发布统计看板，按平台展示发文总数、近 7 天新增、待发布、发布失败、已被 AI 引用、关联用户意图和待再次监测数量
    - 平台明细页展示该平台的内容列表、账号列表、素材规则、发文频率、最近发布时间和下一步建议
    - 内容列表支持按发布状态、内容类型、优化单元、用户意图、是否被引用和是否已复测筛选
    - 每条内容提供查看草稿、进入发布准备、记录发布结果、创建再次监测、查看引用表现入口
    - 覆盖需求 6.1-6.5、9.3、9.4；对应用户补充“不同媒体发文章的一些统计”

  - [x] 6.5 实现发布记录到 AI 引用和再次监测的聚合关系
    - 后端聚合 `PublishingRecord`、`ContentAsset`、`CitationSource`、`OptimizationTask` 和 `RetestRecord`，生成每个平台和每条内容的发布后表现摘要
    - 发布统计只展示可追溯的发布记录和引用记录，无法识别来源时使用“未识别来源”状态并提供人工补充入口
    - 再次监测状态按未安排、待执行、已执行、已改善、未改善展示，并能跳转到任务复测页面
    - 覆盖需求 6.4、9.3、9.5、10.3；对应设计 `SprintRetestService` 和 `PublishingCenterPage`

  - [x]* 6.6 编写发布运营测试
    - 覆盖自有媒体和媒体平台双模式切换、平台规则展示、发文统计计算和账号缺失提示
    - 覆盖发布准备阶段字段缺失提示和再次监测入口
    - 覆盖需求 6.1-6.5

- [x] 7. 统一分析诊断页面和可执行建议
  - [x] 7.1 建设分析诊断通用容器
    - 新增或扩展 `AnalysisWorkbench`，统一顶部诊断卡、趋势区、平台筛选、明细区和任务区
    - 诊断卡展示本期关键发现、影响范围、建议动作、风险等级和下一步按钮
    - 竞品、评价、信源、事实分析均复用该容器
    - 覆盖需求 7.1、8.1、9.1、10.1、12.4；对应设计 `Page-Level Design/分析诊断页面`

  - [x] 7.2 补齐四类分析聚合接口
    - 在 shared-types 中定义 `AnalysisFinding`、`AnalysisWorkbenchDashboard`、`AnalysisRecommendedAction` 类型
    - 在后端增加或扩展四类分析聚合接口：竞品、评价、信源、事实，底层复用 `AnalysisResult`、`Competitor`、`CitationSource`、`EvaluationIssue`、`BrandProfile` 和内容任务数据
    - 聚合结果必须包含优化单元、用户意图、平台、证据、严重程度、建议动作和关联任务 ID
    - 覆盖需求 7.1-7.4、8.1-8.4、9.1-9.5、10.1-10.4；对应设计 `Backend Interfaces` 和 `Data Models/AnalysisFinding`

  - [x] 7.3 实现分析明细到任务的动作入口
    - 竞品差距支持创建竞品改进任务并关联优化单元、竞品、用户意图和内容资产类型
    - 负向评价支持事实澄清、内容补强、FAQ 补充和复测任务入口
    - 高频信源支持创建官网页、FAQ、评测页或问答内容建议
    - 事实偏差支持更新标准答案、创建内容任务和安排再次监测
    - 覆盖需求 7.4、8.3、9.4、10.3、10.4

  - [x]* 7.4 编写分析聚合和 UI 测试
    - 覆盖四类分析页顶部诊断卡、平台筛选、明细展示和建议动作
    - 覆盖未识别来源、事实依据缺失、负向评价和竞品压制四类边界状态
    - 覆盖需求 7.1-10.4 和错误处理 `引用缺失`、`事实冲突`

- [x] 8. 完成全局空态、状态和视觉系统重构
  - [x] 8.1 统一空态和下一步动作组件
    - 扩展 `PageState.tsx` 或新增业务空态组件，统一展示当前缺少的数据、为什么需要它、建议下一步和主按钮
    - 替换品牌资料、优化单元、用户意图、AI 回复监测、内容、发布、分析页面中的泛化空态
    - 覆盖需求 1.4、2.6、4.5、6.5、9.5、10.4、12.3；对应错误处理 `资料缺失`、`平台未配置`、`引用缺失`

  - [x] 8.2 建立新手友好 UI 设计规范落地层
    - 更新 `global.css` 和关键页面布局，统一任务卡、诊断卡、列表工具栏、状态标签、侧栏配置、右侧结果面板和移动端响应式约束
    - 控制卡片层级，避免页面区块过度卡片化，列表页使用工具栏、筛选、表格和行操作承接主要工作
    - 内容创作页保持左配置右结果，分析页保持顶部诊断卡、中部趋势、底部明细
    - 覆盖需求 1.1、5.1、5.3、7.1、8.1、9.1、10.1、12.4；对应用户对竞品设计清晰度的补充要求

  - [x] 8.3 落地关键页面 UI 线框级结构
    - 新手首页线框：顶部品牌与当前阶段、三步起步任务、今日待办、核心指标、推荐下一步、模块入口矩阵
    - 监测对象页线框：顶部解释与主按钮、搜索筛选工具栏、指标摘要、对象列表、行操作、右侧详情抽屉
    - 内容运营页线框：内容任务列表、模板筛选、发文统计摘要、左配置右结果创作台、发布检查面板
    - 发布运营页线框：平台统计卡、账号状态、渠道规则、内容发布列表、复测状态、平台明细抽屉
    - 分析诊断页线框：顶部诊断结论、平台/对象筛选、趋势区、明细表、建议任务区
    - 覆盖需求 1.1、3.1、5.1、6.3、7.1、12.4；对应用户补充“详细、精细、全面的 UI 重新优化”

  - [x] 8.4 执行公开 UI 用词和内部字段治理
    - 扫描前端公开文案，移除 demo、mock、内部 code、拼音平台名、内部 ID、伪链接和工程化状态文本
    - 将状态枚举转为业务状态，将平台 code 转为业务平台名，将技术字段收纳到详情抽屉或隐藏
    - 覆盖需求 12.1-12.5；对应正确性属性 5、6

  - [x]* 8.5 编写全局 UI 治理测试
    - 增加关键页面文案测试，覆盖平台显示、空态引导、状态标签和内部字段隐藏
    - 覆盖需求 12.1-12.5；对应 Web 测试 `工作台模块入口展示`、`品牌资料库缺失项引导`

  - [x] 8.6 精简全平台公开文案
    - 统一页面标题、功能说明、按钮、状态、空状态和错误提示的表达方式
    - 保留优化单元、用户意图、AI 回复监测等既有业务术语，清理公开页面中的 code、ID、Prompt、provider、fallback 和实现细节
    - 补充共享文案测试，覆盖需求 12.6-12.9

  - [x] 8.7 依据参考产品统一业务板块命名
    - 将一级导航更新为工作台、品牌信息、内容中心、发布中心和数据分析
    - 统一数据总览、营销画布、品牌信息、竞品信息、AI 平台管理、优化建议和发布记录的导航入口与页面标题
    - 保留 AI 回复监测、内容资产、顾问服务和再次监测等项目特有能力名称，并保持现有路由及工作流参数
    - 更新导航测试和项目架构文档，覆盖需求 13.1-13.5

- [x] 9. 检查点 - 确保所有测试通过
  - 确保所有测试通过,如有疑问请询问用户

- [x] 10. 补齐后端持久化、品牌隔离和安全边界
  - [x] 10.1 扩展 shared-types 和 repository port
    - 为品牌资料库、媒体资产、内容资产、媒体账号、平台规则、发文统计、分析 finding 和页面聚合 dashboard 增加共享类型
    - 在 `PermissionsRepositoryPort` 或对应模块 repository port 中补齐读取、创建、更新和聚合查询方法
    - 所有新增业务记录保留 `brandId` 并复用现有品牌访问策略
    - 覆盖需求 2.1、5.5、6.1、7.4、8.4、9.4、10.3；对应 beginner-friendly 属性 B1 和设计正确性属性 3、4

  - [x] 10.2 定义后端页面聚合 DTO 和字段映射
    - 定义 `BeginnerHomeDashboard`，聚合品牌资料完整度、监测对象数量、真实回复状态、内容任务、发布统计、分析风险和下一步动作
    - 定义 `MonitoringObjectDashboard`，聚合优化单元、用户意图、监测问题、推荐度、平均排名、引用率、内容任务和复测状态
    - 定义 `ContentOperationDashboard`，聚合内容任务、模板、素材、发布准备、发布统计和再次监测状态
    - 定义 `PublishingOperationDashboard`，聚合媒体账号、平台规则、内容发布记录、AI 引用、发布后表现和待复测事项
    - 定义 `AnalysisDiagnosisDashboard`，聚合竞品、评价、信源、事实四类 finding 和可执行建议
    - 覆盖需求 1.1、3.2、5.1、6.3、7.1、8.1、9.1、10.1；对应设计 `Backend Interfaces`

  - [x] 10.3 实现 memory repository 聚合数据路径
    - 在内存仓储中实现 profile-library、owned-media、content-assets、analysis dashboards 和 publishing stats 的聚合读取与写入
    - 补充 demo 数据，使新手首页、内容运营、媒体发布统计和分析诊断能在无数据库环境展示完整路径
    - 覆盖需求 1.1、2.1、5.1、6.1、7.1、8.1、9.1、10.1

  - [x] 10.4 实现 Prisma repository 聚合数据路径
    - 基于现有 Prisma 模型或轻量新增模型补齐资料库、媒体资产、发布统计和分析 finding 的持久化读取
    - 如需新增 Prisma 模型，创建迁移并保持 `npm run prisma:validate` 和 `npm run prisma:generate` 通过
    - 覆盖需求 2.5、5.5、6.2、6.4、7.4、10.3；对应设计 `Data Models`

  - [x] 10.5 实现 BFF 聚合控制器和服务层
    - 在品牌、内容、发布或新增 dashboard 模块中提供页面聚合 API，保持统一 `ApiResponse<T>` 响应结构
    - 控制器通过 `x-brand-id` 和现有品牌访问 middleware 校验品牌访问权限
    - 服务层只做聚合、映射、状态翻译和下一步动作生成，底层 CRUD 继续复用现有模块仓储
    - 聚合接口失败时返回用户可理解错误码和 fallback 空态所需字段
    - 覆盖需求 1.1、2.6、4.5、5.1、6.3、7.1、12.4；对应架构文档 API 响应约定

  - [x] 10.6 强化公开响应脱敏和真实指标边界
    - 检查平台配置、浏览器会话、LLM 任务、自动化任务包、Sprint 聚合和新增页面聚合接口的响应字段
    - 公开响应只返回脱敏状态、状态摘要、关联 ID、业务标签和用户可理解原因
    - 指标聚合层继续只读取真实 AI 回复、浏览器辅助结果或手动录入真实回复
    - 覆盖需求 4.5、11.3、11.4、12.5；对应正确性属性 1、2、4

  - [x]* 10.7 编写后端 API、repository 和安全属性测试
    - 覆盖新增聚合接口的品牌隔离、memory/Prisma 一致性、公开响应脱敏和缺失数据 fallback
    - Property P6：任意新增业务记录必须包含 `brandId` 并通过品牌访问校验，覆盖 beginner-friendly 属性 B1
    - Property P7：任意公开平台配置响应不得包含真实密钥、cookies、storage state 或 browser profile 路径，覆盖需求 11.3 和正确性属性 4

- [x] 11. 完成端到端页面串联和回归验证
  - [x] 11.1 串联新手路径的页面跳转和动作链
    - 确保“补资料 -> 建优化单元 -> 建用户意图 -> 开始监测 -> 看诊断 -> 生成内容 -> 发媒体 -> 再次监测”每一步都有可见入口
    - 关键行操作统一跳到对应页面并携带可复用上下文参数或 hash 锚点
    - 覆盖需求 1.5、3.5、4.1、5.5、6.4、7.4、10.3

  - [x] 11.2 更新路由、懒加载和预览配置
    - 检查 `routePaths.ts`、`routes.tsx`、`WorkspaceRouteRedirect.tsx` 和导航别名，确保新增或复用页面都能访问
    - 保持 Vite `/api` 代理和 `.monkeycode-ai.online` allowedHosts 配置
    - 覆盖需求 1.2、12.4；对应架构文档前端路由约定

  - [x] 11.3 执行交付验证命令
    - 在 `当前工作区` 运行 `npm run verify`
    - 运行 `git diff --check`
    - 如 Prisma schema 有变更，确认 `npm run prisma:validate` 和 `npm run prisma:generate` 在 verify 中通过
    - 覆盖设计 `Verification Commands`

  - [x]* 11.4 编写自动化端到端测试
    - 覆盖新手首轮路径：新手首页、品牌资料、优化单元、用户意图、AI 回复监测、内容生成、发布准备和再次监测入口
    - 覆盖分析异常到内容任务、事实修正和复测入口
    - 覆盖需求 1.1、1.5、4.1、5.5、6.4、7.4、10.3

- [x] 12. 检查点 - 确保所有测试通过
  - 确保所有测试通过,如有疑问请询问用户
