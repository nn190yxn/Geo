# 需求实施计划

- [x] 1. 建立视觉令牌和共享页面模板
  - [x] 1.1 重构全局视觉令牌和基础布局样式
    - 修改 `apps/web/src/styles/global.css`，定义页面宽度、桌面/平板/移动边距、区域间距、组件间距、圆角、边界、阴影、文字层级和语义色变量
    - 收口现有 `geo-*`、`page-*` 和 Ant Design 覆盖样式，建立稳定的页面背景、内容面板、紧凑列表和数据区域视觉层级
    - 保持 Vite、Ant Design 和现有主题加载方式不变
    - 覆盖需求 7.1、7.3、7.4；对应设计 `Visual System/Layout Tokens`、`Hierarchy` 和 `Color Roles`

  - [x] 1.2 实现统一页面标题和内容骨架
    - 新增 `ProductPage`、`ProductPageHeader` 和页面内容分区组件，统一标题、用途说明、品牌上下文、主操作、辅助操作和最大阅读宽度
    - 为主操作、辅助操作和上下文区域定义明确接口，支持现有页面渐进迁移
    - 将加载、部分数据和错误提示插槽纳入页面骨架
    - 覆盖需求 1.1、7.1、7.2、8.3；对应设计 `Components and Interfaces/ProductPage` 和 `State Model`

  - [x] 1.3 实现行动型空状态和页面状态组件
    - 扩展 `apps/web/src/components/PageState.tsx`，实现 `GuidedEmptyState`、区域错误状态、骨架状态和部分数据提示
    - 空状态固定承载原因、业务影响、收益说明、一个主动作和可选辅助说明
    - 为请求重试、补品牌资料、开始监测、创建内容和录入发布结果提供可复用动作映射
    - 覆盖需求 3.2、5.3、6.3、8.3；对应设计 `GuidedEmptyState`、`State Model` 和 `Error Handling`

  - [x] 1.4 实现统一筛选条、平台切换和指标概览组件
    - 新增 `UnifiedFilterBar`、`PlatformSwitch`、`MetricSummaryGrid`、`InsightOverview` 和明细区域骨架
    - 统一搜索、时间、平台、状态、结果计数和清空筛选的布局与 URL query 接口
    - 平台切换固定显示豆包、Kimi、DeepSeek、通义千问和阶跃星辰，并支持全部平台
    - 覆盖需求 3.1、3.4、5.1、5.2、7.1；对应设计 `UnifiedFilterBar`、`PlatformSwitch` 和 `InsightOverview`

  - [x] 1.5 实现双栏创建、资产库和管理列表模板
    - 新增 `CreationWorkspace`，支持左侧分组配置、右侧预期说明/生成状态/结果预览和移动端分步排列
    - 新增 `AssetLibrary`，支持左侧分类导航、右侧表单或列表、完整度摘要和主操作
    - 新增 `ManagementListPage`，统一标题、筛选、单一主操作、表格、行操作和分页区域
    - 覆盖需求 3.1-3.3、4.2、6.1-6.3；对应设计 `CreationWorkspace`、`AssetLibrary` 和 `Page Mapping`

  - [x]* 1.6 编写共享页面模板单元和组件测试
    - 覆盖页面标题、主操作、空状态、筛选 query、平台切换、双栏布局和五类页面状态
    - 验证共享模板在缺少可选区域时保持稳定结构
    - 覆盖需求 3.2、7.1-7.3、8.3；对应设计 `Test Strategy/Unit Tests` 和 `Component Tests`

  - [x]* 1.7 编写 Property P6 主操作唯一性属性测试
    - Property P6：任意页面首屏最多存在一个实心主色按钮
    - 对共享页面模板生成不同主操作、辅助操作和状态组合，验证主操作视觉语义唯一
    - 覆盖需求 1.1、7.2；对应设计正确性属性 6

- [x] 2. 重构任务型应用壳和全局导航
  - [x] 2.1 将一级导航收敛为五个任务域
    - 修改 `apps/web/src/layouts/navigation.ts`，按开始、监测、优化、发布和分析组织全部现有业务页面
    - 为品牌资料、竞品资料、AI 平台、关系视图、内容资产、媒体账号、任务、报告、反馈和顾问页面定义稳定的二级入口
    - 保留现有 route path、品牌化别名和 lazy route 注册
    - 覆盖需求 1.2、8.1；对应设计 `AppShell` 和 `Page Mapping`

  - [x] 2.2 重构桌面端应用壳和品牌上下文
    - 修改 `apps/web/src/layouts/AppLayout.tsx`，实现可折叠侧栏、统一全局头部、当前任务信息和品牌切换
    - 将页面标题和说明下沉到 `ProductPage`，全局头部只保留品牌、导航控制和必要账户上下文
    - 减少菜单同时展开的入口数量，并为当前任务域保留清晰选中状态
    - 覆盖需求 1.1、1.2、7.4；对应设计 `Components and Interfaces/AppShell`

  - [x] 2.3 实现移动端抽屉导航和响应式应用壳
    - 在宽度小于 768px 时使用抽屉导航，保持品牌切换、当前页面和主操作可访问
    - 为侧栏折叠、抽屉开关、路由切换和焦点返回实现稳定交互
    - 保证页面内容区在 390px、768px、1024px 和 1440px 宽度下使用对应边距令牌
    - 覆盖需求 1.4、8.2、8.4；对应设计 `AppShell` 和 `Visual Review`

  - [x] 2.4 将全局八步流程改为场景化相邻步骤
    - 替换始终显示的 `workflow-strip`，仅在主链路页面展示当前阶段、上一阶段和下一阶段
    - 复用 `routePaths.ts` 保留优化单元、用户意图、监测问题、运行记录、内容任务、发布记录和复测任务上下文
    - 在高级分析、资料管理和支持工具页面隐藏流程导航，减少首屏干扰
    - 覆盖需求 1.3、8.1；对应设计 `AppShell` 和正确性属性 4

  - [x]* 2.5 编写导航、应用壳和深链接回归测试
    - 覆盖五个任务域、全部现有页面入口、品牌切换、桌面折叠和移动抽屉行为
    - 覆盖品牌化别名、未知别名回退和主链路相邻步骤
    - 覆盖需求 1.2-1.4、8.1、8.4；对应设计 `Test Strategy/Component Tests` 和 `Regression Tests`

  - [x]* 2.6 编写 Property P4 工作流上下文保持属性测试
    - Property P4：任意现有工作流链接在重构后保留 query 和 hash 上下文
    - 对全部 route builder 生成不同对象 ID、模式和锚点组合，验证构造、重定向和解析结果一致
    - 覆盖需求 3.4、8.1；对应设计正确性属性 4

- [x] 3. 检查点 - 确保所有测试通过
  - 确保所有测试通过,如有疑问请询问用户

- [x] 4. 重构开始使用域和新手首页
  - [x] 4.1 将新手首页重构为首个成功动作入口
    - 拆分 `BrandWorkspacePage.tsx` 中的新手首页、品牌管理、资料导入和 Sprint 展示职责
    - 首屏只展示一个推荐主任务、三个阶段和一个主按钮，并根据资料、监测和真实回复状态生成下一步
    - 将高级运营工作台、完整流程、品牌管理和自动化运营员收纳到二级 Tab、折叠区或独立页面
    - 覆盖需求 2.1、2.4、7.2；对应设计 `Page Mapping/新手首页`

  - [x] 4.2 实现有数据首页的四项摘要和问题式入口
    - 在已有真实监测结果时展示推荐度、平均排名、引用率和待处理问题四项摘要
    - 使用问题式入口承载“AI 怎么评价我的品牌”“哪些回答需要修正”“下一篇内容写什么”等高频目标
    - 点击示例问题时携带品牌、问题和目标区块上下文进入监测或分析页面
    - 覆盖需求 2.2、2.3；对应设计 `Page Mapping/新手首页` 和正确性属性 3、4

  - [x] 4.3 重构多品牌总览和品牌建档入口
    - 将品牌列表、创建、编辑和状态切换迁移到独立管理区域，使用品牌资产卡和单一创建入口
    - 品牌卡只展示名称、状态、资料完整度、首轮监测状态和一个高频动作
    - 将低频操作收纳到更多菜单，并保留当前品牌切换逻辑
    - 覆盖需求 3.3、6.3、7.2；对应设计 `Design Principles` 和 `Page Mapping`

  - [x] 4.4 重构竞品资料和 AI 平台接入页面
    - 将 `CompetitorAnalysisPage` 的竞品档案维护能力拆分为任务清晰的竞品资料管理视图
    - 将 `ModelSettingsPage` 和平台配置区域改为平台卡片、连接状态、可用监测方式和唯一连接/管理动作
    - 公开区域只展示脱敏连接状态、最近验证结果和用户可理解的下一步
    - 覆盖需求 6.1-6.4、7.2；对应设计 `平台接入` 页面模板和正确性属性 2

  - [x]* 4.5 编写新手首页和开始使用域组件测试
    - 覆盖未建档、待建监测对象、待采集真实回复、已有结果和存在风险五类首页状态
    - 覆盖问题式入口、品牌卡操作、竞品资料空状态和平台连接状态
    - 覆盖需求 2.1-2.4、6.3、8.3；对应设计 `Test Strategy/Component Tests`

  - [x]* 4.6 编写 Property P2 平台配置公开边界属性测试
    - Property P2：任意公开平台配置区域只展示脱敏状态和用户可理解原因
    - 对不同连接模式、校验状态和异常载荷验证页面树不包含密钥、cookies、storage state 或 browser profile 路径
    - 覆盖需求 6.4；对应设计正确性属性 2

- [x] 5. 重构品牌资料和统一资产库
  - [x] 5.1 将品牌资料页改造成分类资产库
    - 重构 `BrandKnowledgeCard.tsx` 和相关 helper，左侧固定展示基础信息、产品服务、目标用户、事实知识和媒体素材分类
    - 右侧按分类展示紧凑表单、可重复条目列表或资产列表
    - 保留资料完整度、缺失项、来源状态和对监测/生成结果的影响说明
    - 覆盖需求 6.1、6.2；对应设计 `AssetLibrary` 和 `Page Mapping/品牌资料`

  - [x] 5.2 重构基础信息、产品服务和目标用户编辑体验
    - 基础信息使用分组表单、字段示例、字数提示和单一保存动作
    - 产品服务使用可重复条目列表，支持新增、编辑、状态和 FAQ 摘要
    - 目标用户使用画像卡片，展示决策阶段、关注问题、常见表达和关联高价值意图
    - 覆盖需求 6.1、6.2、6.4；对应设计 `AssetLibrary` 和 `Error Handling/品牌资料不足`

  - [x] 5.3 重构事实知识和媒体素材管理体验
    - 事实知识使用左侧分组、顶部搜索筛选和右侧列表，展示来源、审核状态和更新时间
    - 媒体素材使用同构资产列表，展示素材类型、适用平台、关联内容和审核状态
    - 为空分类提供上传、手动录入或示例结构主操作
    - 覆盖需求 6.2、6.3；对应设计 `AssetLibrary` 和 `GuidedEmptyState`

  - [x] 5.4 收口资料保存、导入确认和下一步反馈
    - 保留现有资料上传、解析、字段确认和手动填写能力
    - 将导入状态、待确认字段和保存反馈统一到资产库右侧工作区
    - 保存后明确展示资料完整度变化及创建优化单元或开始监测的下一步
    - 覆盖需求 6.3、6.4、8.3；对应设计 `Error Handling/品牌资料不足`

  - [x]* 5.5 编写品牌资产库组件和状态测试
    - 覆盖分类切换、表单保存、可重复条目、资料导入、空分类、完整度和保存反馈
    - 覆盖桌面左侧分类与移动端分类选择行为
    - 覆盖需求 6.1-6.4、8.4；对应设计 `Test Strategy/Component Tests`

- [x] 6. 重构监测对象、AI 回复监测和关系视图
  - [x] 6.1 将优化单元和用户意图统一为管理列表模板
    - 重构 `OptimizationUnitsCard.tsx` 和 `UserIntentPromptCard.tsx`，统一标题说明、搜索筛选、单一新增入口、表格和行动型空状态
    - 每行只保留两个高频操作，将生成内容、查看引用、编辑和低频动作收纳到更多菜单
    - 保留优化单元、用户意图、监测问题和路由上下文关联
    - 覆盖需求 3.1-3.4、7.2；对应设计 `ManagementListPage` 和 `Page Mapping`

  - [x] 6.2 将 AI 回复监测重构为结论到证据的看板
    - 重构 `MonitoringPage.tsx`，按关键结论、四项指标、平台切换、趋势或分布、监测问题和回复明细组织页面
    - 将开始监测设为页面唯一主操作，将平台配置和手动录入作为当前状态下的辅助路径
    - 将监测主题、问题候选、计划执行和回复记录通过 Tab 或分段区域渐进展开
    - 覆盖需求 5.1、5.2、5.4、7.2；对应设计 `InsightOverview` 和 `Page Mapping/AI 回复监测`

  - [x] 6.3 统一真实回复不足、待确认和失败状态
    - 将缺少真实回复、浏览器待确认、手动待录入、平台待配置和运行失败映射为一致的状态摘要和恢复动作
    - 缺少真实样本时展示样本范围、影响指标和开始监测入口
    - 保留自动监测、浏览器辅助监测和手动录入三条真实回复路径
    - 覆盖需求 5.3、8.3；对应设计 `State Model` 和 `Error Handling/样本不足`

  - [x] 6.4 将关系视图定位为高级分析画布
    - 重构 `GeoCanvasPage.tsx` 的页面标题、工具栏、图例、缩放定位、节点说明和首次使用引导
    - 保留左侧对象、中央关系图和右侧详情结构，并减少首屏行内操作
    - 从节点详情继续提供查看真实回复、生成内容和再次监测入口
    - 覆盖需求 3.3、5.4、8.3；对应设计 `Design Principles` 和 `Page Mapping`

  - [x]* 6.5 编写监测对象和监测看板组件测试
    - 覆盖列表筛选、行操作收纳、平台切换、样本不足、部分数据、运行失败和恢复动作
    - 覆盖关系视图图例、节点详情和工作流跳转
    - 覆盖需求 3.1-3.4、5.1-5.4、8.3；对应设计 `Test Strategy/Component Tests`

  - [x]* 6.6 编写 Property P1 品牌上下文属性测试
    - Property P1：任意重构页面的主数据查询必须携带当前 `brandId`
    - 对开始、监测、内容、发布、分析和支持工具页面生成不同品牌上下文，验证请求路径或 `x-brand-id` 注入一致
    - 覆盖需求 8.1；对应设计正确性属性 1

  - [x]* 6.7 编写 Property P3 真实指标边界属性测试
    - Property P3：任意指标摘要只能使用真实 AI 回复、浏览器辅助结果或手动录入真实回复
    - 对 API、浏览器、手动、`mock_ai`、空回复、标准答案和内容草稿组合验证首页与监测摘要语义一致
    - 覆盖需求 2.2、5.3；对应设计正确性属性 3

- [x] 7. 检查点 - 确保所有测试通过
  - 确保所有测试通过,如有疑问请询问用户

- [x] 8. 重构内容运营域
  - [x] 8.1 将内容生成列表改造成统一任务管理页
    - 重构 `ContentGenerationPage.tsx` 顶部任务区域，使用统一筛选条、状态筛选、结果计数、单一创建入口和管理表格
    - 任务行展示标题、模板、关联对象、目标平台、生成状态、发布时间和两个高频操作
    - 为空列表提供选择模板并创建第一条内容的行动型空状态
    - 覆盖需求 3.1-3.3、4.1；对应设计 `ManagementListPage` 和 `Page Mapping/内容生成`

  - [x] 8.2 实现内容模板选择流程
    - 将现有内容模板规则改造成按品牌宣传、问答、案例、教程、对比、科普和渠道内容组织的模板选择界面
    - 模板卡展示使用场景、适用平台、预计结构、素材要求和选择状态
    - 选择模板后进入双栏创建工作台并写入现有任务输入
    - 覆盖需求 4.1、4.2；对应设计 `CreationWorkspace` 和 `Page Mapping/内容生成`

  - [x] 8.3 重构左配置右结果内容创作台
    - 左侧按目标与对象、模板与渠道、素材与依据、生成配置和发布检查分组
    - 右侧统一展示生成前预期、生成中步骤、失败恢复、草稿预览、风险提示和发布准备动作
    - 移动端按配置、结果两个步骤排列并保留生成主操作
    - 覆盖需求 4.2-4.4、8.3、8.4；对应设计 `CreationWorkspace` 和 `Error Handling/生成失败`

  - [x] 8.4 重构内容优化和内容资产页面
    - 将内容优化改为粘贴或选择现有内容、配置优化目标、查看结构/事实/FAQ/引用/渠道建议的双栏工作台
    - 将内容资产改为统一管理列表或资产库，支持搜索、类型、平台、审核、发布和复测状态筛选
    - 保留继续编辑、进入发布准备和再次监测动作
    - 覆盖需求 3.1-3.4、4.2-4.4；对应设计 `CreationWorkspace`、`ManagementListPage` 和 `Page Mapping`

  - [x]* 8.5 编写内容运营域组件测试
    - 覆盖模板选择、任务筛选、双栏状态、配置校验、生成失败恢复、草稿动作和内容资产筛选
    - 覆盖从内容任务到发布准备的 query 上下文
    - 覆盖需求 3.1-3.4、4.1-4.4、8.1；对应设计 `Test Strategy/Component Tests`

- [x] 9. 重构发布运营域
  - [x] 9.1 将自有媒体和媒体平台改造成接入与规则页面
    - 自有媒体使用平台卡片或账号列表展示账号名称、授权状态、最近验证和唯一管理动作
    - 媒体平台使用规则列表展示内容格式、适用意图、发布频率、素材要求和注意事项
    - 将接入、重新授权和状态异常统一为可执行状态反馈
    - 覆盖需求 3.1-3.3、7.2、8.3；对应设计 `平台接入` 和 `ManagementListPage`

  - [x] 9.2 将发布准备改造成统一发布记录工作台
    - 重构 `PublishingCenterPage.tsx`，默认展示状态筛选、发布记录、内容摘要、账号、计划时间、真实链接和再次监测状态
    - 保留内容生成交接定位、真实发布链接录入、发布状态保存和再次监测任务创建
    - 将账号和平台规则收纳到独立入口或二级 Tab
    - 覆盖需求 3.1-3.4、8.1、8.3；对应设计 `Page Mapping/发布准备`

  - [x] 9.3 将再次监测任务改造成行动型任务列表
    - 重构 `TaskRetestPage.tsx`，按待处理、待复测、已改善和继续优化组织任务状态
    - 每行展示来源发布记录、原问题、负责人、计划时间、前后指标和当前下一步
    - 保留携带原问题返回 AI 回复监测页面执行同题复测的入口
    - 覆盖需求 3.1-3.4、5.4、8.1；对应设计 `ManagementListPage` 和正确性属性 4

  - [x]* 9.4 编写发布运营域组件测试
    - 覆盖账号状态、平台规则、发布记录筛选、真实链接录入、发布状态和再次监测任务创建
    - 覆盖发布到复测的上下文保持和失败恢复
    - 覆盖需求 3.1-3.4、8.1、8.3；对应设计 `Test Strategy/Component Tests`

- [x] 10. 重构分析诊断域
  - [x] 10.1 建立统一分析页面骨架和筛选语义
    - 将竞品、评价、信源、事实和增长优化页面统一为标题说明、分析范围、平台切换、关键结论、趋势/分布和证据明细结构
    - 统一时间、平台、优化单元、用户意图和状态筛选位置
    - 将分析结果中的高频动作放入明细行，将低频动作收纳到更多菜单
    - 覆盖需求 3.1、3.3、5.1、5.2、7.2；对应设计 `InsightOverview` 和 `Page Mapping`

  - [x] 10.2 重构竞品分析和评价分析页面
    - 竞品分析先展示提及率、推荐排名和压制风险，再展示平台矩阵、趋势和高风险意图明细
    - 评价分析先展示正向、中性、负向和准确表达摘要，再展示趋势、表达问题和修正动作
    - 保留地图发现竞品、创建修正策略和更新品牌知识库能力
    - 覆盖需求 5.1、5.2、5.4；对应设计 `InsightOverview` 和 `Design Principles`

  - [x] 10.3 重构信源分析和事实分析页面
    - 信源分析先展示引用率、官网引用率和权威来源占比，再展示来源分布、趋势和引用明细
    - 事实分析先展示事实风险摘要，再展示失真信息、证据、用户意图和可执行修正建议
    - 对空数据和样本不足分别展示采集路径和补资料路径
    - 覆盖需求 5.1-5.4、8.3；对应设计 `InsightOverview` 和 `Error Handling/样本不足`

  - [x] 10.4 重构增长优化计划和统一诊断工作台
    - 将 `GrowthOptimizationPage.tsx` 按优先问题、原因证据、推荐动作、关联内容和复测状态组织
    - 将分析诊断聚合页的四类 finding 统一为结论卡、严重程度、证据摘要和任务化入口
    - 保留生成内容任务、更新标准答案、安排发布和再次监测动作
    - 覆盖需求 5.1、5.4、8.1；对应设计 `InsightOverview` 和 `Page Mapping`

  - [x]* 10.5 编写分析诊断域组件测试
    - 覆盖统一筛选、平台切换、关键结论、趋势、证据明细、空数据和任务化动作
    - 覆盖竞品、评价、信源、事实和增长优化页面的现有业务入口
    - 覆盖需求 5.1-5.4、8.3；对应设计 `Test Strategy/Component Tests`

- [x] 11. 重构报告、顾问、反馈和自动化支持页面
  - [x] 11.1 将报告中心改造成统一管理列表和详情阅读页
    - 使用管理列表模板展示报告类型、品牌、周期、生成状态、数据缺口和创建时间
    - 将报告正文、数据缺口和导出动作放入独立详情区域
    - 为空列表提供生成首份品牌报告的主操作
    - 覆盖需求 3.1-3.3、7.2、8.3；对应设计 `ManagementListPage`

  - [x] 11.2 将顾问工作台改造成服务任务和记录视图
    - 使用统一筛选和任务列表组织诊断、服务计划、复盘、客户交付、培训和跟进事项
    - 详情区域展示关联报告、状态、负责人、时间和下一步
    - 将新增服务记录设为页面唯一主操作
    - 覆盖需求 3.1-3.3、7.2；对应设计 `ManagementListPage` 和 `ProductPage`

  - [x] 11.3 重构内测反馈和自动化运营员界面
    - 将反馈页面统一为反馈类型、页面、严重程度、状态和处理记录列表
    - 将自动化运营员的任务包、步骤进度和确认队列迁移到统一页面骨架和状态组件
    - 高风险确认、手动录入和失败恢复使用一致的行动型状态
    - 覆盖需求 3.1-3.3、8.3；对应设计 `ProductPage`、`State Model` 和 `GuidedEmptyState`

  - [x]* 11.4 编写支持工具页面组件测试
    - 覆盖报告列表与详情、顾问任务与记录、反馈筛选和自动化确认状态
    - 覆盖加载、空数据、部分失败和请求失败状态
    - 覆盖需求 3.1-3.3、8.3；对应设计 `Test Strategy/Component Tests`

- [x] 12. 完成全页面响应式、可访问性和路由收口
  - [x] 12.1 为全部页面模板实现桌面、平板和移动布局
    - 验证开始、监测、内容、发布、分析和支持工具页面在 1440px、1024px、768px 和 390px 宽度的布局规则
    - 双栏页面在小屏转换为分步或纵向结构，管理列表保留主操作和关键字段，资产库分类转换为可展开选择
    - 移除固定宽度导致的横向溢出，并为宽表格提供明确滚动容器
    - 覆盖需求 1.4、7.4、8.4；对应设计 `Visual Review` 和正确性属性 5

  - [x] 12.2 完善键盘操作、焦点和语义标签
    - 为抽屉导航、Tab、筛选器、模板卡、更多菜单、弹窗和主要动作补齐可见焦点和语义标签
    - 保证弹窗关闭后焦点返回触发元素，页面跳转后焦点进入页面标题或主内容
    - 为图标按钮、状态颜色和图表数据提供文字说明
    - 覆盖需求 8.2；对应设计 `Design Principles` 和 `Visual Review`

  - [x] 12.3 收口全页面加载、空态、部分数据和错误状态
    - 将全部业务页面迁移到统一 `loading`、`ready`、`empty`、`partial` 和 `error` 状态模型
    - API 失败时保留可用区域和用户输入，并提供重试或恢复动作
    - 清理重复空状态、重复页面标题、重复主按钮和工程化状态文案
    - 覆盖需求 7.2、8.3；对应设计 `State Model` 和 `Error Handling`

  - [x] 12.4 复核全部路由、懒加载和预览配置
    - 更新 `routes.tsx`、`routePaths.ts`、`WorkspaceRouteRedirect.tsx` 和导航别名，确保全部重构页面可通过现有路径访问
    - 保留 Vite `/api` 代理、`.monkeycode-ai.online` allowedHosts 和页面 lazy loading
    - 保留品牌化深链接、query 和 hash 解析语义
    - 覆盖需求 8.1、8.4；对应设计 `Regression Tests` 和正确性属性 4

  - [x]* 12.5 编写 Property P5 响应式主任务一致性属性测试
    - Property P5：任意核心页面在桌面和移动布局中存在同一主任务入口
    - 对全部页面模板和主要状态组合验证主操作标签、目标路由和可访问名称一致
    - 覆盖需求 1.4、8.4；对应设计正确性属性 5

  - [x]* 12.6 编写自动化全页面回归和视觉状态测试
    - 覆盖全部导航路由、品牌化别名、主要 query/hash、加载、空态、部分数据、完整数据和失败状态
    - 覆盖桌面、平板和移动视口下的关键页面结构与主任务
    - 覆盖需求 1.1-1.4、7.1-7.4、8.1-8.4；对应设计 `Test Strategy/Regression Tests` 和 `Visual Review`

- [x] 13. 检查点 - 确保所有测试通过
  - 确保所有测试通过,如有疑问请询问用户

- [x] 14. 执行最终代码质量门禁
  - [x] 14.1 执行完整 workspace 验证
    - 在 `当前工作区` 运行 `npm run verify`
    - 确认 Web typecheck、现有测试、生产构建、Prisma schema 校验和 Prisma Client 生成通过
    - 运行 `git diff --check` 并修复本次重构引入的格式问题
    - 覆盖需求 8.1-8.4；对应设计 `Test Strategy/Regression Tests`
